---
title: "Astro"
description: "Astro is a repository module detected from 1 source file."
order: 40
---

# Astro

Astro is a repository module detected from 1 source file.
## What This Module Owns
Astro owns the behavior described by the source-backed function docs and implementation facts below. The module key is `adapters/astro`, and the evidence comes from `src/adapters/astro.ts`.
## What It Actually Does
- `betterAuthStudio(config: StudioConfig)` - Astro adapter for Better Auth Studio Usage in a catch-all API route: ```ts // pages/api/studio/[...all].ts import { betterAuthStudio } from 'better-auth-studio/astro'; import studioConfig from '../../../../studio.config'; import type { APIRoute } from 'astro'; const handler = betterAuthStudio(studioConfig); export const ALL: APIRoute = async (ctx) => { return handler(ctx); }; ```

## Behavior From Source Evidence
- Recognizes evidence markers such as `Content-Type`.
- Maps detected inputs to normalized values such as `Content-Type` -> `application/json`.
## Module Responsibilities
- Adapters
- Astro
- betterAuthStudio
## Public or Observable Surface
The items below are extracted from the files attached to this page. Treat them as evidence for what the module exposes or handles, not as a guarantee that every symbol is public API.
- `betterAuthStudio`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `src/adapters/astro.ts` - Framework adapter that connects host requests to the product runtime.

### Knowledge Chunks
- `src/adapters/astro.ts:1-35` - betterAuthStudio (source; TypeScript; module adapters/astro; symbols betterAuthStudio). import { handleStudioRequest } from "../core/handler.js"; import type { StudioConfig, UniversalRequest, UniversalResponse } from "../types/handler.js"; import { injectEventHooks, injectLastSeenAtHooks } from "../utils/hook injector.js"; / Astro adapter for Better Auth Studio Usage in a catch all API route: / export function betterAuthStudio(config: StudioConfig) { if (config.auth) { injectLastSeenAtHooks(config.auth,
- `src/adapters/astro.ts:35-115` - betterAuthStudio (source; TypeScript; module adapters/astro; symbols betterAuthStudio). return new Response(JSON.stringify({ error: "Internal server error" }), { status: 500, headers: { "Content Type": "application/json" }, }); } }; } async function convertAstroToUniversal( ctx: { request: Request }, config: StudioConfig, ): Promise { const request = ctx.request; let body: any; const method = request.method; if (method !== "GET" && method !== "HEAD") { const contentType = request.headers.get("content ty

### Source Details
- `src/adapters/astro.ts`: language TypeScript; exports `betterAuthStudio`

### Evidence
- `src/adapters/astro.ts`
