---
title: "Bulk Operations"
description: "The product includes bulk creation, seeding, import, or export operations."
order: 430
---

# Bulk Operations

The product includes bulk creation, seeding, import, or export operations.
## What This Is
Bulk operations help operators or developers create, update, export, or seed many records at once. This page explains safety boundaries because bulk actions can change a lot of data quickly.

Repository context: Important: For self hosting, install better auth studio as a regular dependency (not devDependency) since it's required at runtime in production:
## Why It Matters
Bulk Operations matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- bulk actions
- seed data
- import
- export
- test records
## Typical Workflow
1. Choose the target record type and operation.
2. Review generated or imported records before committing changes.
3. Use bulk actions primarily for controlled admin tasks, demos, testing, or migrations.
## Detailed Guide Goal
Bulk Operations takes a reader from a known starting point to a useful end state. The guide is more than a command list: it explains when to use the workflow, what state the project needs before starting, what each step changes, and how to verify that the workflow succeeded.

The guide is written for someone who wants to accomplish a task, not someone browsing the source code. Commands and flags appear as tools inside the workflow while the reader stays oriented around the goal.

## When to Use This
- Use this guide when a developer needs to perform the bulk operations workflow from the project root or host application.
- Use it when a maintainer needs to understand the intended operator or developer path before changing behavior.
- Link to API or configuration pages only after the reader understands the task.

## Success Criteria
- The reader knows the prerequisite state.
- The reader knows what command, action, or setting moves them forward.
- The reader knows the expected result and how to verify it.
## Before You Start
- Install dependencies with `pnpm install`.
- Confirm the main workflow with `pnpm start`.
- Review the maintainer reference before adding new examples.
## Command Mental Model
- `build` runs `pnpm run clean && tsc && pnpm run frontend:build && pnpm run copy:public && pnpm run verify:public` and creates the production build or compiled package artifacts.
- `geo:update` runs `node scripts/download-geolite2.js` and supports the project workflow.
- `copy:public` runs `rm -rf dist/public && cp -r public dist/` and supports the project workflow.
- `verify:public` runs `test -f dist/public/index.html && echo '✓ Public directory copied successfully' || (echo '✗ ERROR: Public directory not copied!' && exit 1)` and supports the project workflow.
- `postinstall` runs `node scripts/postinstall.js` and supports the project workflow.
- `dev` runs `tsx src/cli.ts` and starts the local development workflow.
- `start` runs `node dist/cli.js` and starts the runtime entrypoint after dependencies or build output are ready.
- `studio` runs `tsx src/cli.ts` and starts the runtime entrypoint after dependencies or build output are ready.
- `clean` runs `rm -rf dist` and removes generated artifacts so the next build starts from a clean state.
- `frontend:dev` runs `cd frontend && pnpm run dev` and starts the local development workflow.
- `frontend:build` runs `cd frontend && pnpm run build` and creates the production build or compiled package artifacts.
- `frontend:preview` runs `cd frontend && pnpm run preview` and supports the project workflow.
- `lint` runs `oxlint . && oxfmt --check .` and checks or fixes code style before changes are shipped.
- `format` runs `oxlint --fix . && oxfmt .` and checks or fixes code style before changes are shipped.
- `lint:fix` runs `oxlint --fix . && oxfmt .` and checks or fixes code style before changes are shipped.
- `type-check` runs `tsc --noEmit` and supports the project workflow.
## Raw Commands and Flags
- `build`: `pnpm run clean && tsc && pnpm run frontend:build && pnpm run copy:public && pnpm run verify:public`
- `geo:update`: `node scripts/download-geolite2.js`
- `copy:public`: `rm -rf dist/public && cp -r public dist/`
- `verify:public`: `test -f dist/public/index.html && echo '✓ Public directory copied successfully' || (echo '✗ ERROR: Public directory not copied!' && exit 1)`
- `postinstall`: `node scripts/postinstall.js`
- `dev`: `tsx src/cli.ts`
- `start`: `node dist/cli.js`
- `studio`: `tsx src/cli.ts`
- `clean`: `rm -rf dist`
- `frontend:dev`: `cd frontend && pnpm run dev`
- `frontend:build`: `cd frontend && pnpm run build`
- `frontend:preview`: `cd frontend && pnpm run preview`
- `lint`: `oxlint . && oxfmt --check .`
- `format`: `oxlint --fix . && oxfmt .`
- `lint:fix`: `oxlint --fix . && oxfmt .`
- `type-check`: `tsc --noEmit`
- `test`: `vitest`
- `test:watch`: `vitest --watch`
- `test:coverage`: `vitest --coverage`
- `test:ci`: `pnpm test --run`
- `release`: `pnpm build && bumpp && pnpm publish --access public --no-git-checks`
- `release:beta`: `pnpm build && bumpp --preid beta && pnpm publish --access public --tag beta --no-git-checks`
- `release:no-build`: `bumpp && pnpm publish --access public --no-git-checks --tag next`
- `release:canary`: `pnpm build && bumpp --preid canary --no-commit --no-tag --no-push && pnpm publish --access public --tag canary --no-git-checks`
- `bump`: `bumpp`
- `--api-dir`: <path>", 'Custom app directory path for Next.js (e.g., "app" or "src/app"). The command will auto-detect "app" or "src/app" if not provided.
- `--config`: <path>", "Path to the auth configuration file
- `--geo-db`: <path>", "Path to MaxMind GeoLite2 database file (.mmdb
- `--host`: <host>", "Host to run the studio on", "localhost
- `--no-open`
- `--port`: <port>", "Port to run the studio on", "3002
- `--watch`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### 🏢 Organization Management
- **View organizations** - List all organizations with pagination
- **Create organizations** - Add new organizations with custom slugs
- **Edit organizations** - Update organization details
- **Delete organizations** - Remove organizations
- **Team management** - Create and manage teams within organizations
- **Member management** - Add/remove members from teams
- **Bulk seeding** - Generate test organizations and teams

### 👥 User Management
- **View all users** - Paginated list with search and filtering
- **Create users** - Add new users with email/password
- **Edit users** - Update user information, email verification status
- **Delete users** - Remove users from the system
- **Bulk operations** - Seed multiple test users
- **User details** - View user profiles, and accounts
- **Last seen** - When events are enabled, the studio injects an optional `lastSeenAt` field on the user model and updates it on each sign-in or session creation. Run your database migration (e.g. `prisma migrate dev` or Drizzle push) to add the `lastSeenAt` column to the user table.

### Installation for Self Hosting
**Important:** For self-hosting, install `better-auth-studio` as a **regular dependency** (not devDependency) since it's required at runtime in production:

```bash
pnpm add better-auth-studio
```

> **Note:** The CLI usage (standalone studio) can be installed as a dev dependency, but self-hosting requires it in `dependencies` for production deployments.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- The guide stays task-oriented: prerequisite, action, expected result, verification.
- Each command or option is connected to its reason before the raw command inventory appears.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `frontend/src/components/ExportAnalyticsModal.tsx` - ExportAnalyticsModal reusable UI component. It likely renders part of the workflow or a focused interaction.

### Knowledge Chunks
- `README.md#organization-management:1-7` - 🏢 Organization Management (readme; Markdown; heading README > 🏢 Organization Management > 🏢 Organization Management). View organizations List all organizations with pagination Create organizations Add new organizations with custom slugs Edit organizations Update organization details Delete organizations Remove organizations Team management Create and manage teams within organizations Member management Add/remove members from teams Bulk seeding Generate test organizations and teams
- `README.md#user-management:1-7` - 👥 User Management (readme; Markdown; heading README > 👥 User Management > 👥 User Management). View all users Paginated list with search and filtering Create users Add new users with email/password Edit users Update user information, email verification status Delete users Remove users from the system Bulk operations Seed multiple test users User details View user profiles, and accounts Last seen When events are enabled, the studio injects an optional lastSeenAt field on the user model and updates it on each si
- `frontend/src/components/ExportAnalyticsModal.tsx:1-13` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). import { format } from "date fns"; import { toPng } from "html to image"; import { Download, Share2 } from "lucide react"; import { useCallback, useEffect, useMemo, useRef, useState } from "react"; import { toast } from "sonner"; import { useCounts } from "@/contexts/CountsContext"; import { assetPath } from "@/lib/utils"; import { Calendar as CalendarIcon, X } from "./PixelIcons"; import { Button } from "./ui/button
- `frontend/src/components/ExportAnalyticsModal.tsx:13-88` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). export type ExportMode = "overview" | "users" | "activity" | "organizations" | "teams"; const INDIVIDUAL MODES: Exclude [] = [ "users", "activity", "organizations", "teams", ]; interface ExportAnalyticsModalProps { open?: boolean; defaultMode?: ExportMode; onClose: () = void; initialMode?: ExportMode; } interface AnalyticsData { data: number[]; labels: string[]; percentageChange: number; total: number; previousTotal?
- `frontend/src/components/ExportAnalyticsModal.tsx:88-121` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). function pickAxisLabels(labels: string[], maxLabels: number): { label: string; index: number }[] { if (labels.length ({ label: l, index: i })); } const result: { label: string; index: number }[] = []; result.push({ label: labels[0], index: 0 }); const inner = maxLabels 2; for (let i = 1; i <= inner; i++) { const idx = Math.round((i / (inner + 1)) (labels.length 1)); result.push({ label: labels[idx], index: idx }); }
- `frontend/src/components/ExportAnalyticsModal.tsx:121-205` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). labels.length === data.length ? labels : data.map(( , i) = String(i + 1)), Math.min(10, barCount), ); return ( {/ Y axis left labels + gridlines /} {yTicks.map((tick) = { const y = chartH (tick / max) (chartH 4); return ( {formatAxisValue(tick)} ); })} {/ Y axis right labels /} {yTicks.map((tick) = { const y = chartH (tick / max) (chartH 4); return ( {formatAxisValue(tick)} ); })} {/ Bars /} {data.map((value, i) = {
- `frontend/src/components/ExportAnalyticsModal.tsx:205-268` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). map(({ label, index }) = { const x = yAxisW + barGap + index (barW + barGap) + barW / 2; return ( {label} ); })} ); } function PercentageBadge({ value }: { value: number }) { const isPositive = value = 0; return ( {Math.abs(value).toFixed(1)}% ); } function CardScaleWrapper({ width, height, children, }: { width: number; height: number; children: React.ReactNode; }) { const containerRef = useRef (null); const [scale,
- `frontend/src/components/ExportAnalyticsModal.tsx:268-321` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). {children} ); } function useImageAsDataUrl(src: string): string | null { const [dataUrl, setDataUrl] = useState (null); useEffect(() = { let cancelled = false; (async () = { try { const response = await fetch(src); const blob = await response.blob(); const reader = new FileReader(); reader.onloadend = () = { if (!cancelled) setDataUrl(reader.result as string); }; reader.readAsDataURL(blob); } catch { / ignore / } })(
- `frontend/src/components/ExportAnalyticsModal.tsx:321-392` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). const [analyticsMap, setAnalyticsMap] = useState ({}); const [loading, setLoading] = useState(false); const [exporting, setExporting] = useState(false); const cardRef = useRef (null); const { counts } = useCounts(); const shaderDataUrl = useImageAsDataUrl(assetPath("/shaders.png")); const logoDataUrl = useImageAsDataUrl(assetPath("/logo.png")); const isAllSelected = INDIVIDUAL MODES.every((m) = selectedModes.has(m));
- `frontend/src/components/ExportAnalyticsModal.tsx:392-403` - ExportAnalyticsModal, ExportMode (source; TypeScript; module frontend/src; symbols ExportAnalyticsModal, ExportMode). as(mode)) continue; for (const stat of FOOTER STAT MAP[mode] || []) { if (!seen.has(stat.key)) { seen.add(stat.key); result.push(stat); } } } return result; }, [selectedModes]);

### Source Details
- `frontend/src/components/ExportAnalyticsModal.tsx`: language TypeScript; exports `ExportAnalyticsModal`, `ExportMode`

### Evidence
- `frontend/src/components/ExportAnalyticsModal.tsx`
- `README.md#organization-management`
- `README.md#user-management`
