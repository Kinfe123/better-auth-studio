---
title: "Database"
description: "The repository includes database schema, migration, or adapter configuration evidence."
order: 360
---

# Database

The repository includes database schema, migration, or adapter configuration evidence.
## What This Is
Database documentation explains how the project connects to persistence, which schema or migration files matter, and what has to be ready before runtime features read or write state.
## Why It Matters
Database matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- database
- schema
- migrations
- adapter
- persistence
## Typical Workflow
1. Identify the database library, schema files, and migration files used by the project.
2. Document required connection variables and migration steps.
3. Explain which runtime features depend on the database being available.
## Detailed Configuration Model
Database controls behavior rather than UI copy. The important questions are what decision this configuration helps a developer make, what happens when it is absent, and what runtime behavior changes when it is enabled. This page keeps the mental model ahead of the raw files that mention the setting.

For Better Auth Studio, configuration usually answers practical questions: who can access the product, where it is mounted, which runtime services it depends on, and what optional capabilities are enabled. Each setting is documented in relation to one of those questions.

## Decision Points
- Is the setting required for every installation, or only for a specific framework, adapter, or deployment mode?
- Does the setting affect security, persistence, presentation, integrations, or developer convenience?
- What does a developer verify after changing it?

## Success Criteria
- Readers know when to configure this area and when they can ignore it.
- Readers understand the consequence of a wrong value.
- Readers can connect the setting to a visible product behavior or runtime integration.
## Configuration Surface
Configuration is easiest to understand when each setting is tied to the behavior it controls. The tables below collect the relevant files and environment variables after the mental model, so readers can connect the concept to the concrete setup surface.
### Files
- `examples/express/drizzle/drizzle.config.ts` (database) - Database schema or adapter configuration.
- `examples/nextjs/drizzle/drizzle.config.ts` (database) - Database schema or adapter configuration.
### Environment Variables
- `AUTH_SECRET`
- `AUTH_URL`
- `BASE_URL`
- `BETTER_AUTH_ALLOWED_HOSTS`
- `BETTER_AUTH_SECRET`
- `BETTER_AUTH_URL`
- `CLERK_SECRET_KEY`
- `CLICKHOUSE_HOST`
- `CLICKHOUSE_PASSWORD`
- `CLICKHOUSE_USERNAME`
- `DATABASE_URL`
- `DB_HOST`
- `DB_NAME`
- `DB_PASSWORD`
- `DB_PORT`
- `DB_USER`
- `DISCORD_CLIENT_ID`
- `DISCORD_CLIENT_SECRET`
- `GITHUB_CLIENT_ID`
- `GITHUB_CLIENT_SECRET`
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`
- `IPINFO_TOKEN`
- `MONGO_URI`
- `MONGODB_DB_NAME`
- `MONGODB_URI`
- `MYSQL_URL`
- `NEXT_PUBLIC_BETTER_AUTH_URL`
- `NODE_ENV`
- `NODE_PATH`
- `PORT`
- `POSTGRES_URL`
- `RESEND_API_KEY`
- `VERCEL`
- `VERCEL_URL`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Supported Database Adapters
Better Auth Studio automatically detects and works with:

- **Prisma** (`prismaAdapter`)
- **Drizzle** (`drizzleAdapter`)
- **SQLite** (`new Database()` from better-sqlite3)
- **PostgreSQL** (via Prisma or Drizzle)
- **MySQL** (via Prisma or Drizzle)
## How to Reason About This Config
- Identify whether the option affects security, runtime mounting, data access, or presentation.
- Prefer examples that use the same names and defaults found in the repository evidence.
- Call out adapter-specific behavior instead of implying every setting applies to every framework.
## Validation Checklist
- Keep config examples aligned with source defaults and runtime validation.
- Mention whether a setting is required, optional, or only needed for a specific adapter.
- Update this page when config object names, env vars, or supported providers change.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Required settings, optional settings, defaults, and adapter-specific behavior are separated clearly.
- Security impact and validation expectations are called out before examples.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/express/drizzle/drizzle.config.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/nextjs/drizzle/drizzle.config.ts` - Example project file that shows how the capability is wired in a real framework setup.

### Knowledge Chunks
- `examples/express/drizzle/drizzle.config.ts:1-12` - Express (example; TypeScript; module examples/express). import { type Config } from "drizzle kit"; type ConfigWithoutDriver = Omit ; export default { schema: "./auth schema.ts", dialect: "postgresql", out: "./drizzle", // @ts ignore dbCredentials: { url: process.env.DATABASE URL, }, } satisfies ConfigWithoutDriver;
- `examples/nextjs/drizzle/drizzle.config.ts:1-11` - Nextjs (example; TypeScript; module examples/nextjs). import type { Config } from "drizzle kit"; export default { schema: "./auth schema.ts", out: "./drizzle", dialect: "postgresql", dbCredentials: { url: process.env.DATABASE URL!, }, } satisfies Config;

### Source Details
- `examples/express/drizzle/drizzle.config.ts`: language TypeScript; env `DATABASE_URL`
- `examples/nextjs/drizzle/drizzle.config.ts`: language TypeScript; env `DATABASE_URL`

### Evidence
- `examples/express/drizzle/drizzle.config.ts`
- `examples/nextjs/drizzle/drizzle.config.ts`
