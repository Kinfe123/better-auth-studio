---
title: "Database Adapters"
description: "The project detects or configures database adapters."
order: 350
---

# Database Adapters

The project detects or configures database adapters.
## What This Is
Database adapter docs explain how the product connects to the persistence layer used by the host application. They identify supported adapters, the expected provider names, and what needs to be migrated before the runtime can read or write records safely.

Repository context: ```typescript // auth. ts import { betterAuth } from "better auth"; import { drizzleAdapter } from "better auth/adapters/drizzle"; import { db } from ".
## Why It Matters
Database Adapters matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- Prisma
- Drizzle
- SQLite
- Postgres
- MySQL
- schema detection
## Typical Workflow
1. Identify which adapter the host application already uses.
2. Confirm that the product can load the adapter and database connection from the host configuration.
3. Run migrations or schema pushes before using features that require additional columns.
## Detailed Configuration Model
Database Adapters controls behavior rather than UI copy. The important questions are what decision this configuration helps a developer make, what happens when it is absent, and what runtime behavior changes when it is enabled. This page keeps the mental model ahead of the raw files that mention the setting.

For Better Auth Studio, configuration usually answers practical questions: who can access the product, where it is mounted, which runtime services it depends on, and what optional capabilities are enabled. Each setting is documented in relation to one of those questions.

## Decision Points
- Is the setting required for every installation, or only for a specific framework, adapter, or deployment mode?
- Does the setting affect security, persistence, presentation, integrations, or developer convenience?
- What does a developer verify after changing it?

## Success Criteria
- Readers know when to configure this area and when they can ignore it.
- Readers understand the consequence of a wrong value.
- Readers can connect the setting to a visible product behavior or runtime integration.
## Configuration Surface
Configuration is easiest to understand when each setting is tied to the behavior it controls. The tables below collect the relevant files and environment variables after the mental model, so readers can connect the concept to the concrete setup surface.
### Files
- `examples/astro/prisma/src/lib/auth.ts` (product) - Product runtime configuration.
- `examples/astro/prisma/studio.config.ts` (product) - Product runtime configuration.
- `examples/elysia/prisma/src/auth.ts` (product) - Product runtime configuration.
- `examples/elysia/prisma/studio.config.ts` (product) - Product runtime configuration.
- `examples/express/drizzle/server/src/lib/auth.ts` (product) - Product runtime configuration.
- `examples/express/drizzle/src/auth.ts` (product) - Product runtime configuration.
- `examples/express/drizzle/studio.config.ts` (product) - Product runtime configuration.
- `examples/express/mongodb/src/auth.ts` (product) - Product runtime configuration.
- `examples/express/prisma/src/auth.ts` (product) - Product runtime configuration.
- `examples/express/prisma/studio.config.ts` (product) - Product runtime configuration.
- `examples/hono/prisma/src/auth.ts` (product) - Product runtime configuration.
- `examples/hono/prisma/studio.config.pg.example.ts` (product) - Product runtime configuration.
- `examples/hono/prisma/studio.config.ts` (product) - Product runtime configuration.
- `examples/nextjs/drizzle/lib/auth.ts` (product) - Product runtime configuration.
- `examples/nextjs/drizzle/studio.config.ts` (product) - Product runtime configuration.
- `examples/nextjs/mongodb/lib/auth.ts` (product) - Product runtime configuration.
### Environment Variables
- `AUTH_SECRET`
- `AUTH_URL`
- `BASE_URL`
- `BETTER_AUTH_ALLOWED_HOSTS`
- `BETTER_AUTH_SECRET`
- `BETTER_AUTH_URL`
- `CLERK_SECRET_KEY`
- `CLICKHOUSE_HOST`
- `CLICKHOUSE_PASSWORD`
- `CLICKHOUSE_USERNAME`
- `DATABASE_URL`
- `DB_HOST`
- `DB_NAME`
- `DB_PASSWORD`
- `DB_PORT`
- `DB_USER`
- `DISCORD_CLIENT_ID`
- `DISCORD_CLIENT_SECRET`
- `GITHUB_CLIENT_ID`
- `GITHUB_CLIENT_SECRET`
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`
- `IPINFO_TOKEN`
- `MONGO_URI`
- `MONGODB_DB_NAME`
- `MONGODB_URI`
- `MYSQL_URL`
- `NEXT_PUBLIC_BETTER_AUTH_URL`
- `NODE_ENV`
- `NODE_PATH`
- `PORT`
- `POSTGRES_URL`
- `RESEND_API_KEY`
- `VERCEL`
- `VERCEL_URL`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Supported Database Adapters
Better Auth Studio automatically detects and works with:

- **Prisma** (`prismaAdapter`)
- **Drizzle** (`drizzleAdapter`)
- **SQLite** (`new Database()` from better-sqlite3)
- **PostgreSQL** (via Prisma or Drizzle)
- **MySQL** (via Prisma or Drizzle)

### Prisma Setup
```typescript
// auth.ts
import { betterAuth } from "better-auth";
import { prismaAdapter } from "better-auth/adapters/prisma";
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

export const auth = betterAuth({
  database: prismaAdapter(prisma, {
    provider: "postgresql", // or "mysql", "sqlite"
  }),
  // ... other config
});
```

### Drizzle Setup
```typescript
// auth.ts
import { betterAuth } from "better-auth";
import { drizzleAdapter } from "better-auth/adapters/drizzle";
import { db } from "./database";

export const auth = betterAuth({
  database: drizzleAdapter(db, {
    provider: "pg", // or "mysql", "sqlite"
  }),
  // ... other config
});
```
## How to Reason About This Config
- Identify whether the option affects security, runtime mounting, data access, or presentation.
- Prefer examples that use the same names and defaults found in the repository evidence.
- Call out adapter-specific behavior instead of implying every setting applies to every framework.
## Validation Checklist
- Keep config examples aligned with source defaults and runtime validation.
- Mention whether a setting is required, optional, or only needed for a specific adapter.
- Update this page when config object names, env vars, or supported providers change.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Required settings, optional settings, defaults, and adapter-specific behavior are separated clearly.
- Security impact and validation expectations are called out before examples.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `./dist/cli.js` - CLI entrypoint or command implementation that defines developer-facing commands and flags.
- `examples/astro/prisma/astro.config.mjs` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/astro/prisma/src/env.d.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/astro/prisma/src/lib/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `examples/astro/prisma/src/lib/db.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/astro/prisma/src/pages/api/auth/[...all].ts` - Route or API layer that receives requests and coordinates runtime behavior.
- `examples/astro/prisma/src/pages/api/studio/[...all].ts` - Route or API layer that receives requests and coordinates runtime behavior.
- `examples/astro/prisma/studio.config.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `examples/elysia/prisma/src/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `examples/elysia/prisma/studio.config.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.

### Knowledge Chunks
- `README.md#drizzle-setup:1-13` - Drizzle Setup (readme; Markdown; heading README > Drizzle Setup > Drizzle Setup). ```typescript // auth.ts import { betterAuth } from "better-auth"; import { drizzleAdapter } from "better-auth/adapters/drizzle"; import { db } from "./database"; export const auth = betterAuth({ database: drizzleAdapter(db, { provider: "pg", // or "mysql", "sqlite" }), // ... other config }); ```
- `README.md#prerequisites:1-5` - 📋 Prerequisites (readme; Markdown; heading README > 📋 Prerequisites > 📋 Prerequisites). Before using Better Auth Studio, ensure you have: Node.js (v18 or higher) A Better Auth project with a valid auth.ts configuration file Database setup (Prisma, Drizzle, or SQLite)
- `README.md#prisma-setup:1-15` - Prisma Setup (readme; Markdown; heading README > Prisma Setup > Prisma Setup). ```typescript // auth.ts import { betterAuth } from "better-auth"; import { prismaAdapter } from "better-auth/adapters/prisma"; import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); export const auth = betterAuth({ database: prismaAdapter(prisma, { provider: "postgresql", // or "mysql", "sqlite" }), // ... other config }); ```
- `README.md#settings-configuration:1-4` - ⚙️ Settings & Configuration (readme; Markdown; heading README > ⚙️ Settings & Configuration > ⚙️ Settings & Configuration). Plugin status Check which Better Auth plugins are enabled Database configuration View current database adapter and settings Social providers Configure OAuth providers (GitHub, Google, etc.) Email settings Configure email verification and password reset
- `README.md#supported-database-adapters:1-7` - Supported Database Adapters (readme; Markdown; heading README > Supported Database Adapters > Supported Database Adapters). Better Auth Studio automatically detects and works with: Prisma (prismaAdapter) Drizzle (drizzleAdapter) SQLite (new Database() from better sqlite3) PostgreSQL (via Prisma or Drizzle) MySQL (via Prisma or Drizzle)
- `README.md#user-management:1-7` - 👥 User Management (readme; Markdown; heading README > 👥 User Management > 👥 User Management). View all users Paginated list with search and filtering Create users Add new users with email/password Edit users Update user information, email verification status Delete users Remove users from the system Bulk operations Seed multiple test users User details View user profiles, and accounts Last seen When events are enabled, the studio injects an optional lastSeenAt field on the user model and updates it on each si
- `./dist/cli.js:1-76` - Cli (source; JavaScript; module dist/cli). !/usr/bin/env node import { existsSync, readFileSync } from "node:fs"; import { basename, dirname, isAbsolute, join, relative } from "node:path"; import { fileURLToPath } from "node:url"; import chalk from "chalk"; import chokidar from "chokidar"; import { Command } from "commander"; import { WebSocket } from "ws"; import { initCommand } from "./cli/commands/init.js"; import { findAuthConfig } from "./config.js"; imp
- `./dist/cli.js:76-96` - Cli (source; JavaScript; module dist/cli). configPath) { return null; } if (isAbsolute(configPath) && existsSync(configPath)) { return configPath; } const absolutePath = join(process.cwd(), configPath); if (existsSync(absolutePath)) { return absolutePath; } return null; } function getFileDisplayInfo(filePath) { if (!filePath) { return { fileName: undefined, filePath: undefined, absolutePath: undefined, }; }
- `./dist/cli.js:96-139` - Cli (source; JavaScript; module dist/cli). const absolutePath = isAbsolute(filePath) ? filePath : join(process.cwd(), filePath); const relativePath = relative(process.cwd(), absolutePath); return { absolutePath, fileName: basename(absolutePath), filePath: relativePath || absolutePath, }; } function formatChangeLabel(fileInfo) { return fileInfo.filePath || fileInfo.fileName || "auth config"; } async function startStudioWithWatch(options) { const { port, host,
- `./dist/cli.js:139-156` - Cli (source; JavaScript; module dist/cli). process.stdout.write(${chalk.yellow(\n↻ Reloading Better Auth Studio (changed ${changeLabel}))}\n); try { broadcastWatchMessage({ type: "studio status", status: "refreshing", updatedAt: Date.now(), fileName: changeInfo.fileName, filePath: changeInfo.filePath, }, { remember: "status" }); broadcastWatchMessage({ type: "config change detected", message: "Auth configuration change detected", fileName: changeInfo.fileName

### Source Details
- `./dist/cli.js`: language JavaScript; flags `--api-dir`, `--config`, `--geo-db`, `--host`, `--no-open`, `--port`
- `examples/astro/prisma/src/lib/auth.ts`: language TypeScript; exports `auth`; env `AUTH_SECRET`, `BASE_URL`, `BETTER_AUTH_URL`
- `examples/astro/prisma/src/lib/db.ts`: language TypeScript; exports `prisma`; env `NODE_ENV`
- `examples/astro/prisma/src/pages/api/auth/[...all].ts`: language TypeScript; exports `ALL`
- `examples/astro/prisma/src/pages/api/studio/[...all].ts`: language TypeScript; exports `ALL`
- `examples/astro/prisma/studio.config.ts`: language TypeScript; exports `config`
- `examples/elysia/prisma/src/auth.ts`: language TypeScript; exports `auth`; env `AUTH_SECRET`, `BETTER_AUTH_URL`, `DISCORD_CLIENT_ID`, `DISCORD_CLIENT_SECRET`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`
- `examples/elysia/prisma/studio.config.ts`: language TypeScript; exports `config`

### Evidence
- `./dist/cli.js`
- `examples/astro/prisma/astro.config.mjs`
- `examples/astro/prisma/src/env.d.ts`
- `examples/astro/prisma/src/lib/auth.ts`
- `examples/astro/prisma/src/lib/db.ts`
- `examples/astro/prisma/src/pages/api/auth/[...all].ts`
- `examples/astro/prisma/src/pages/api/studio/[...all].ts`
- `examples/astro/prisma/studio.config.ts`
- `examples/elysia/prisma/src/auth.ts`
- `examples/elysia/prisma/studio.config.ts`
- `examples/express/drizzle/drizzle.config.ts`
- `README.md#drizzle-setup`
