---
title: "Organization Management"
description: "The project includes organizations, teams, members, or invitation workflows."
order: 180
---

# Organization Management

The project includes organizations, teams, members, or invitation workflows.
## What This Is
This capability documents the multi-tenant administrative model. It explains how organizations group users, how teams subdivide access, and how membership or invitation actions change what a person can do inside the product.
## Why It Matters
Organization Management matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- organizations
- teams
- members
- invitations
- slugs
## Typical Workflow
1. Inspect organizations from the organization list.
2. Open an organization to manage teams, members, and pending invitations.
3. Use create, edit, delete, and bulk seed flows to keep test or production organizations aligned with the auth model.
## Detailed Behavior
Organization Management works as a product workflow with three layers. The first layer is the user-facing experience: what an operator sees, what they can search or inspect, and what actions they can take. The second layer is the domain model: which records, relationships, or configuration values the workflow depends on. The third layer is the safety model: which permissions, access rules, or validation steps prevent accidental or unauthorized changes.

For Better Auth Studio, this feature is easiest to understand from the operator's point of view. A reader can understand what problem this page solves, what state it reads or changes, and what a successful outcome looks like before they need to think about source files.

## Common Scenarios
- A new operator wants to understand what organization management is for before using it in production.
- A developer is wiring the feature into a host application and needs to know which data, config, or permissions are involved.
- A maintainer changed the feature and needs to update docs without forcing readers to understand the source tree first.

## Success Criteria
- Readers can explain what organization management does in plain language.
- Readers know the main objects or settings involved before they see any file paths.
- Readers know which actions are safe, which actions are destructive, and which actions depend on access control.
## How It Fits in the Product
Organization Management is a product capability, not just a list of source files. The important story is the user-facing outcome, the data or configuration it depends on, and the guardrails that make the workflow safe to operate.
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### 🏢 Organization Management
- **View organizations** - List all organizations with pagination
- **Create organizations** - Add new organizations with custom slugs
- **Edit organizations** - Update organization details
- **Delete organizations** - Remove organizations
- **Team management** - Create and manage teams within organizations
- **Member management** - Add/remove members from teams
- **Bulk seeding** - Generate test organizations and teams
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- User-facing state, available actions, empty/error states, and permission boundaries are part of the product behavior.
- The page explains what data the feature reads or changes before referencing source files.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `frontend/src/components/dashboard-widgets/RecentOrganizationsWidget.tsx` - RecentOrganizationsWidget reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/RecentTeamsWidget.tsx` - RecentTeamsWidget reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/pages/OrganizationDetails.tsx` - OrganizationDetails page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/Organizations.tsx` - Organizations page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/TeamDetails.tsx` - TeamDetails page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/Teams.tsx` - Teams page-level UI. This is where users usually encounter the capability in the product.

### Knowledge Chunks
- `README.md#dashboard:1` - 📊 Dashboard (readme; Markdown; heading README > 📊 Dashboard > 📊 Dashboard). Overview statistics User, teams and organization counts data
- `README.md#organization-management:1-7` - 🏢 Organization Management (readme; Markdown; heading README > 🏢 Organization Management > 🏢 Organization Management). View organizations List all organizations with pagination Create organizations Add new organizations with custom slugs Edit organizations Update organization details Delete organizations Remove organizations Team management Create and manage teams within organizations Member management Add/remove members from teams Bulk seeding Generate test organizations and teams
- `frontend/src/components/dashboard-widgets/RecentOrganizationsWidget.tsx:1-65` - RecentOrganizationsWidget (source; TypeScript; module frontend/src; symbols RecentOrganizationsWidget). import { useEffect, useState } from "react"; import { useNavigate } from "react router dom"; import { format } from "date fns"; import { Building2 } from "../PixelIcons"; interface Org { id: string; name?: string; slug?: string; createdAt?: string; memberCount?: number; } export function RecentOrganizationsWidget() { const navigate = useNavigate(); const [orgs, setOrgs] = useState ([]); const [loading, setLoading] =
- `frontend/src/components/dashboard-widgets/RecentOrganizationsWidget.tsx:65-99` - RecentOrganizationsWidget (source; TypeScript; module frontend/src; symbols RecentOrganizationsWidget). Members Created {orgs.slice(0, 10).map((o) = ( navigate(/organizations/${o.id})} className="border b border white/5 hover:bg white/[3%] cursor pointer transition colors group" {o.name || o.slug || "—"} {o.memberCount ?? "—"} {o.createdAt ? format(new Date(o.createdAt), "MMM dd, HH:mm") : "—"} ))} )} ); }
- `frontend/src/components/dashboard-widgets/RecentTeamsWidget.tsx:1-61` - RecentTeamsWidget (source; TypeScript; module frontend/src; symbols RecentTeamsWidget). import { useEffect, useState } from "react"; import { useNavigate } from "react router dom"; import { format } from "date fns"; interface Team { id: string; name?: string; slug?: string; organizationId?: string; createdAt?: string; } export function RecentTeamsWidget() { const navigate = useNavigate(); const [teams, setTeams] = useState ([]); const [loading, setLoading] = useState(true); useEffect(() = { let cancelle
- `frontend/src/components/dashboard-widgets/RecentTeamsWidget.tsx:61-91` - RecentTeamsWidget (source; TypeScript; module frontend/src; symbols RecentTeamsWidget). Created {teams.slice(0, 10).map((t) = ( navigate(/teams/${t.id})} className="border b border white/5 hover:bg white/[3%] cursor pointer transition colors group" {t.name || t.slug || "—"} {t.createdAt ? format(new Date(t.createdAt), "MMM dd, HH:mm") : "—"} ))} )} ); }
- `frontend/src/pages/OrganizationDetails.tsx:1-87` - OrganizationDetails (source; TypeScript; module frontend/src; symbols OrganizationDetails). import { format, formatDistanceToNow } from "date fns"; import { ArrowUpRight, Edit } from "lucide react"; import { useEffect, useState } from "react"; import { Link, useNavigate, useParams } from "react router dom"; import { toast } from "sonner"; import { AnimatedNumber } from "../components/AnimatedNumber"; import { CopyableId } from "../components/CopyableId"; import { ArrowLeft, Building2, Calendar, Database, Lo
- `frontend/src/pages/OrganizationDetails.tsx:87-123` - OrganizationDetails (source; TypeScript; module frontend/src; symbols OrganizationDetails). const [activeTab, setActiveTab] = useState ( "details", ); const [teamsEnabled, setTeamsEnabled] = useState(false); const [organizationEnabled, setOrganizationEnabled] = useState(false); const [showInviteModal, setShowInviteModal] = useState(false); const [showSeedMembersModal, setShowSeedMembersModal] = useState(false); const [showSeedTeamsModal, setShowSeedTeamsModal] = useState(false); const [showCreateTeamModal,
- `frontend/src/pages/OrganizationDetails.tsx:123-210` - OrganizationDetails (source; TypeScript; module frontend/src; symbols OrganizationDetails). const [removingMembers, setRemovingMembers] = useState ({}); const [teamSeedingLogs, setTeamSeedingLogs] = useState ([]); const [isTeamSeeding, setIsTeamSeeding] = useState(false); const [isUpdating, setIsUpdating] = useState(false); interface User { id: string; name: string; email: string; image?: string; emailVerified: boolean; } useEffect(() = { if (orgId) { fetchOrganization(); checkOrganizationEnabled(); checkTe
- `frontend/src/pages/OrganizationDetails.tsx:210-282` - OrganizationDetails (source; TypeScript; module frontend/src; symbols OrganizationDetails). error("Failed to check teams status:", error); setTeamsEnabled(false); } }; const checkOrganizationEnabled = async () = { try { const response = await fetch("/api/plugins/organization/status"); const data = await response.json(); setOrganizationEnabled(data.enabled); } catch (error) { console.error("Failed to check organization status:", error); setOrganizationEnabled(false); } }; const fetchTeams = async () = { try

### Source Details
- `frontend/src/components/dashboard-widgets/RecentOrganizationsWidget.tsx`: language TypeScript; exports `RecentOrganizationsWidget`
- `frontend/src/components/dashboard-widgets/RecentTeamsWidget.tsx`: language TypeScript; exports `RecentTeamsWidget`
- `frontend/src/pages/OrganizationDetails.tsx`: language TypeScript; exports `OrganizationDetails`
- `frontend/src/pages/Organizations.tsx`: language TypeScript; exports `Organizations`
- `frontend/src/pages/TeamDetails.tsx`: language TypeScript; exports `TeamDetails`
- `frontend/src/pages/Teams.tsx`: language TypeScript; exports `Teams`

### Evidence
- `frontend/src/components/dashboard-widgets/RecentOrganizationsWidget.tsx`
- `frontend/src/components/dashboard-widgets/RecentTeamsWidget.tsx`
- `frontend/src/pages/OrganizationDetails.tsx`
- `frontend/src/pages/Organizations.tsx`
- `frontend/src/pages/TeamDetails.tsx`
- `frontend/src/pages/Teams.tsx`
- `README.md#dashboard`
- `README.md#organization-management`
