---
title: "Dashboard Overview"
description: "The project exposes dashboard or overview UI for operational visibility."
order: 150
---

# Dashboard Overview

The project exposes dashboard or overview UI for operational visibility.
## What This Is
This area gives operators a high-level view of the system before they drill into individual records. It usually brings together counts, recent activity, database state, and health signals so an admin can understand whether the product is working as expected.
## Why It Matters
Dashboard Overview matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- workspace status
- operational metrics
- recent activity
- dashboard widgets
## Typical Workflow
1. Open the dashboard to see the current state of the product.
2. Use summary widgets to identify the area that needs attention.
3. Navigate from a widget into the detailed feature page or record list.
## Detailed Behavior
Dashboard Overview works as a product workflow with three layers. The first layer is the user-facing experience: what an operator sees, what they can search or inspect, and what actions they can take. The second layer is the domain model: which records, relationships, or configuration values the workflow depends on. The third layer is the safety model: which permissions, access rules, or validation steps prevent accidental or unauthorized changes.

For Better Auth Studio, this feature is easiest to understand from the operator's point of view. A reader can understand what problem this page solves, what state it reads or changes, and what a successful outcome looks like before they need to think about source files.

## Common Scenarios
- A new operator wants to understand what dashboard overview is for before using it in production.
- A developer is wiring the feature into a host application and needs to know which data, config, or permissions are involved.
- A maintainer changed the feature and needs to update docs without forcing readers to understand the source tree first.

## Success Criteria
- Readers can explain what dashboard overview does in plain language.
- Readers know the main objects or settings involved before they see any file paths.
- Readers know which actions are safe, which actions are destructive, and which actions depend on access control.
## How It Fits in the Product
Dashboard Overview is a product capability, not just a list of source files. The important story is the user-facing outcome, the data or configuration it depends on, and the guardrails that make the workflow safe to operate.
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### 📊 Dashboard
- **Overview statistics** - User, teams and organization counts data
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- User-facing state, available actions, empty/error states, and permission boundaries are part of the product behavior.
- The page explains what data the feature reads or changes before referencing source files.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/nextjs/prisma/app/dashboard/[...path]/route.ts` - Route page-level UI. This is where users usually encounter the capability in the product.
- `examples/nextjs/prisma/app/dashboard/route.ts` - Route page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/components/dashboard-widgets/DashboardCustomizePanel.tsx` - DashboardCustomizePanel reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx` - DashboardFloatingPanel reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DashboardWidgetGrid.tsx` - DashboardWidgetGrid reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DatabaseWidget.tsx` - DatabaseWidget reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DropTargetSlot.tsx` - DropTargetSlot reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/EventsWidget.tsx` - EventsWidget reusable UI component. It likely renders part of the workflow or a focused interaction.

### Knowledge Chunks
- `README.md#dashboard:1` - 📊 Dashboard (readme; Markdown; heading README > 📊 Dashboard > 📊 Dashboard). Overview statistics User, teams and organization counts data
- `package.json:1-64` - Package Config (package; JSON; heading Package). { "name": "better auth studio", "version": "1.1.3 beta.49", "description": "Studio for Better Auth", "keywords": [ "admin", "authentication", "better auth", "dashboard", "gui", "studio" ], "license": "MIT", "author": "kinfish", "bin": { "better auth studio": "./dist/cli.js" }, "files": [ "README.md", "data/default geo.json", "dist/", "scripts/postinstall.js" ], "type": "module", "main": "dist/index.js", "exports": {
- `package.json:64-116` - Package Config 2 (package; JSON; heading Package). "release:canary": "pnpm build && bumpp preid canary no commit no tag no push && pnpm publish access public tag canary no git checks", "bump": "bumpp" }, "dependencies": { "@babel/preset react": "^7.27.1", "@babel/preset typescript": "^7.27.1", "@better auth/utils": "^0.3.0", "@noble/hashes": "^2.0.1", "@number flow/react": "^0.5.10", "@types/chokidar": "^2.1.7", "better auth": "1.4.5", "bumpp": "^10.2.3", "c12": "^3.
- `frontend/src/components/dashboard-widgets/DashboardCustomizePanel.tsx:1-50` - DashboardCustomizePanel (source; TypeScript; module frontend/src; symbols DashboardCustomizePanel). import { useState } from "react"; import { Button } from "@/components/ui/button"; import { X } from "../PixelIcons"; import { useDashboardWidgets, WIDGET LABELS, type WidgetType, } from "@/contexts/DashboardWidgetsContext"; export function DashboardCustomizePanel() { const { widgets, addWidget, availableToAdd, resetToDefault, setCustomizing, removeWidget } = useDashboardWidgets(); const [addingType, setAddingType] =
- `frontend/src/components/dashboard-widgets/DashboardCustomizePanel.tsx:50-108` - DashboardCustomizePanel (source; TypeScript; module frontend/src; symbols DashboardCustomizePanel). className="bg white/5 border border white/10 text white text sm px 3 py 2 rounded none focus:outline none focus:ring 1 focus:ring white/30" Remove which widget? {widgets.map((w) = ( {WIDGET LABELS[w.widgetType]} (remove) ))} setAddingType(e.target.value as WidgetType | "")} className="bg white/5 border border white/10 text white text sm px 3 py 2 rounded none focus:outline none focus:ring 1 focus:ring white/30" Add w
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx:1-61` - DashboardFloatingPanel, PANEL_WIDTH (source; TypeScript; module frontend/src; symbols DashboardFloatingPanel, PANEL_WIDTH). import type React from "react"; import { useCallback, useState } from "react"; import { GripVertical, PanelRightClose, PanelRightOpen, X } from "lucide react"; import { useDatabaseSchemaSummary } from "@/hooks/useDatabaseSchemaSummary"; import { useDashboardWidgets } from "@/contexts/DashboardWidgetsContext"; import { WIDGET LABELS } from "@/contexts/DashboardWidgetsContext"; import { WIDGET TYPE DRAG KEY } from "./D
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx:61-136` - DashboardFloatingPanel, PANEL_WIDTH (source; TypeScript; module frontend/src; symbols DashboardFloatingPanel, PANEL_WIDTH). {r.time} ))} ); } function PreviewDatabase() { const { tables, loading } = useDatabaseSchemaSummary(); const previewTables = tables.slice(0, 4); if (loading) { return ( Loading ); } if (previewTables.length === 0) { return ( No tables ); } return ( {previewTables.map((table, i) = ( {table.name} {typeof table.rowCount === "number" ? table.rowCount.toLocaleString() : "—"} ))} {tables.length previewTables.length && ( +{
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx:136-186` - DashboardFloatingPanel, PANEL_WIDTH (source; TypeScript; module frontend/src; symbols DashboardFloatingPanel, PANEL_WIDTH). { name: "Alice Chen", email: "alice@acme.io", country: "🇺🇸", joined: "2m ago" }, { name: "Yuki Tanaka", email: "yuki@dev.jp", country: "🇯🇵", joined: "1h ago" }, { name: "Max Weber", email: "max@web.de", country: "🇩🇪", joined: "3h ago" }, ]; return ( Name Email &nbsp; Joined {rows.map((r, i) = ( {r.name} {r.email} {r.country} {r.joined} ))} ); } function PreviewRecentOrgs() { const rows = [ { name: "Acme Corp",
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx:186-238` - DashboardFloatingPanel, PANEL_WIDTH (source; TypeScript; module frontend/src; symbols DashboardFloatingPanel, PANEL_WIDTH). className="text [7px] font mono text gray 600 uppercase tracking wider w 10 text right" Created {rows.map((r, i) = ( ))} ); } function PreviewRecentTeams() { const rows = [ { name: "Engineering", created: "Mar 01" }, { name: "Design", created: "Feb 28" }, { name: "Marketing", created: "Feb 22" }, ]; return ( Name Created {rows.map((r, i) = ( ))} ); } function PreviewWorldMap() { const countries = [ { flag: "🇺🇸", na
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx:238-310` - DashboardFloatingPanel, PANEL_WIDTH (source; TypeScript; module frontend/src; symbols DashboardFloatingPanel, PANEL_WIDTH). full bg white/25" style={{ width: ${c.pct}% }} / ))} ); } const PREVIEW COMPONENTS: Record React.ReactNode = { events: PreviewEvents, database: PreviewDatabase, invitations: PreviewInvitations, "recent users": PreviewRecentUsers, "recent organizations": PreviewRecentOrgs, "recent teams": PreviewRecentTeams, "world map": PreviewWorldMap, }; / / / Main panel / / / export function DashboardFloatingPanel() { const { widg

### Source Details
- `examples/nextjs/prisma/app/dashboard/[...path]/route.ts`: language TypeScript; exports `DELETE`, `GET`, `PATCH`, `POST`, `PUT`
- `examples/nextjs/prisma/app/dashboard/route.ts`: language TypeScript; exports `DELETE`, `GET`, `PATCH`, `POST`, `PUT`
- `frontend/src/components/dashboard-widgets/DashboardCustomizePanel.tsx`: language TypeScript; exports `DashboardCustomizePanel`
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx`: language TypeScript; exports `DashboardFloatingPanel`, `PANEL_WIDTH`
- `frontend/src/components/dashboard-widgets/DashboardWidgetGrid.tsx`: language TypeScript; exports `DashboardWidgetGrid`
- `frontend/src/components/dashboard-widgets/DatabaseWidget.tsx`: language TypeScript; exports `DatabaseWidget`
- `frontend/src/components/dashboard-widgets/DropTargetSlot.tsx`: language TypeScript; exports `DropTargetSlot`, `WIDGET_TYPE_DRAG_KEY`
- `frontend/src/components/dashboard-widgets/EventsWidget.tsx`: language TypeScript; exports `EventsWidget`

### Evidence
- `examples/nextjs/prisma/app/dashboard/[...path]/route.ts`
- `examples/nextjs/prisma/app/dashboard/route.ts`
- `frontend/src/components/dashboard-widgets/DashboardCustomizePanel.tsx`
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx`
- `frontend/src/components/dashboard-widgets/DashboardWidgetGrid.tsx`
- `frontend/src/components/dashboard-widgets/DatabaseWidget.tsx`
- `frontend/src/components/dashboard-widgets/DropTargetSlot.tsx`
- `frontend/src/components/dashboard-widgets/EventsWidget.tsx`
- `package.json:keywords`
- `README.md#dashboard`
