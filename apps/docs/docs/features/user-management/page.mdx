---
title: "User Management"
description: "The project includes user listing, profile, creation, update, or deletion workflows."
order: 200
---

# User Management

The project includes user listing, profile, creation, update, or deletion workflows.
## What This Is
This capability is the administrative surface for user records. It helps an operator inspect accounts, find a user by identity, update profile or verification state, and remove records when needed. In an auth-oriented product, this page is usually one of the highest-value docs pages because it explains the exact model an admin interacts with.
## Why It Matters
User Management matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- users
- accounts
- profiles
- email verification
- last seen activity
## Typical Workflow
1. Find a user through the list, search, or filter UI.
2. Open the user detail view to inspect profile, account, and session-related data.
3. Apply mutations such as create, edit, delete, or bulk seed only when the configured access policy allows it.
## Detailed Behavior
User Management works as a product workflow with three layers. The first layer is the user-facing experience: what an operator sees, what they can search or inspect, and what actions they can take. The second layer is the domain model: which records, relationships, or configuration values the workflow depends on. The third layer is the safety model: which permissions, access rules, or validation steps prevent accidental or unauthorized changes.

For Better Auth Studio, this feature is easiest to understand from the operator's point of view. A reader can understand what problem this page solves, what state it reads or changes, and what a successful outcome looks like before they need to think about source files.

## Common Scenarios
- A new operator wants to understand what user management is for before using it in production.
- A developer is wiring the feature into a host application and needs to know which data, config, or permissions are involved.
- A maintainer changed the feature and needs to update docs without forcing readers to understand the source tree first.

## Success Criteria
- Readers can explain what user management does in plain language.
- Readers know the main objects or settings involved before they see any file paths.
- Readers know which actions are safe, which actions are destructive, and which actions depend on access control.
## How It Fits in the Product
User Management is a product capability, not just a list of source files. The important story is the user-facing outcome, the data or configuration it depends on, and the guardrails that make the workflow safe to operate.
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### 👥 User Management
- **View all users** - Paginated list with search and filtering
- **Create users** - Add new users with email/password
- **Edit users** - Update user information, email verification status
- **Delete users** - Remove users from the system
- **Bulk operations** - Seed multiple test users
- **User details** - View user profiles, and accounts
- **Last seen** - When events are enabled, the studio injects an optional `lastSeenAt` field on the user model and updates it on each sign-in or session creation. Run your database migration (e.g. `prisma migrate dev` or Drizzle push) to add the `lastSeenAt` column to the user table.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- User-facing state, available actions, empty/error states, and permission boundaries are part of the product behavior.
- The page explains what data the feature reads or changes before referencing source files.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/tanstack-start/drizzle/src/routes/account/$path.tsx` - Route or API layer that receives requests and coordinates runtime behavior.
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx` - RecentUsersWidget reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/pages/UserDetails.tsx` - UserDetails page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/Users.tsx` - Users page-level UI. This is where users usually encounter the capability in the product.

### Knowledge Chunks
- `README.md#configuration-options:1-11` - Configuration Options (readme; Markdown; heading README > Configuration Options > Configuration Options). | Option | Required | Description | | | | | | auth | Yes | Your Better Auth instance | | basePath | Yes | URL path where studio is mounted | | access.allowEmails | No | Array of admin email addresses | | access.roles | No | Array of allowed user roles | | access.allowIpAddresses | No | IP allowlist for Studio requests. Supports exact IPs and wildcard patterns (example: 192.168. ) | | access.blockIpAddresses | No | IP
- `README.md#dashboard:1` - 📊 Dashboard (readme; Markdown; heading README > 📊 Dashboard > 📊 Dashboard). Overview statistics User, teams and organization counts data
- `README.md#try-better-auth-studio-demo:1-8` - ✨ Try Better Auth Studio Demo (readme; Markdown; heading README > ✨ Try Better Auth Studio Demo > ✨ Try Better Auth Studio Demo). You can try a live, demo version of Better Auth Studio here: 👉 https://bt nextjs.vercel.app/admin Login email: admin@user.com Password: admin@user.com This online demo lets you explore the admin studio UI. Please note this is a test environment.
- `README.md#user-management:1-7` - 👥 User Management (readme; Markdown; heading README > 👥 User Management > 👥 User Management). View all users Paginated list with search and filtering Create users Add new users with email/password Edit users Update user information, email verification status Delete users Remove users from the system Bulk operations Seed multiple test users User details View user profiles, and accounts Last seen When events are enabled, the studio injects an optional lastSeenAt field on the user model and updates it on each si
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx:1-75` - RecentUsersWidget (source; TypeScript; module frontend/src; symbols RecentUsersWidget). import { useEffect, useMemo, useRef, useState } from "react"; import { useNavigate } from "react router dom"; import { format } from "date fns"; import { ChevronDown, Users } from "../PixelIcons"; interface RecentUser { id: string; name?: string | null; email?: string | null; createdAt?: string; country?: string; countryCode?: string; } function countryFlag(code: string): string { if (!code || code.length !== 2) retu
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx:75-80` - RecentUsersWidget (source; TypeScript; module frontend/src; symbols RecentUsersWidget). if (dropdownRef.current && !dropdownRef.current.contains(e.target as Node)) { setShowDropdown(false); } } document.addEventListener("mousedown", handleClickOutside);
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx:80-147` - RecentUsersWidget (source; TypeScript; module frontend/src; symbols RecentUsersWidget). return () = document.removeEventListener("mousedown", handleClickOutside); }, []); const effectiveHours = useMemo(() = { if (isCustomRange && localFrom) { const fromDate = new Date(localFrom); const toDate = localTo ? new Date(localTo) : new Date(); return Math.max(1, (toDate.getTime() fromDate.getTime()) / 3600000); } return Math.min(8760, Math.max(1, selectedHours)); }, [selectedHours, isCustomRange, localFrom, loc
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx:147-157` - RecentUsersWidget (source; TypeScript; module frontend/src; symbols RecentUsersWidget). ocalFrom, localTo || undefined); setShowDropdown(false); } return ( {/ Header /}
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx:157-200` - RecentUsersWidget (source; TypeScript; module frontend/src; symbols RecentUsersWidget). Recent Users setShowDropdown((v) = !v)} className="flex items center gap 1 text [10px] font mono text gray 500 hover:text white transition colors uppercase" {activeLabel} {showDropdown && ( {PRESET OPTIONS.map((p) = ( selectPreset(p.hours)} className={block w full text left px 3 py 1.5 text [11px] font mono border b border white/5 transition colors ${ !isCustomRange && selectedHours === p.hours ? "text white bg white
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx:200-250` - RecentUsersWidget (source; TypeScript; module frontend/src; symbols RecentUsersWidget). To&nbsp;&nbsp; setLocalTo(e.target.value)} className="flex 1 bg white/5 border border white/10 text white text [10px] font mono px 1.5 py 1 rounded none focus:outline none focus:border white/30 min w 0" / Apply )} {activeLabel} · {total} total {loading ? ( Loading... ) : users.length === 0 ? ( No users in this period ) : ( Name Email {!compact && (

### Source Details
- `examples/tanstack-start/drizzle/src/routes/account/$path.tsx`: language TypeScript; exports `Route`
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx`: language TypeScript; exports `RecentUsersWidget`
- `frontend/src/pages/UserDetails.tsx`: language TypeScript; exports `UserDetails`
- `frontend/src/pages/Users.tsx`: language TypeScript; exports `Users`

### Evidence
- `examples/tanstack-start/drizzle/src/routes/account/$path.tsx`
- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx`
- `frontend/src/pages/UserDetails.tsx`
- `frontend/src/pages/Users.tsx`
- `README.md#configuration-options`
- `README.md#dashboard`
- `README.md#try-better-auth-studio-demo`
- `README.md#user-management`
