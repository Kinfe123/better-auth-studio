---
title: "Developer Tools"
description: "The project includes developer-facing tools, generators, inspectors, or visual editors."
order: 160
---

# Developer Tools

The project includes developer-facing tools, generators, inspectors, or visual editors.
## What This Is
This section explains the utilities that help developers inspect, configure, or generate parts of the product. These tools are not just UI screens; they are shortcuts for common setup, debugging, schema, email, or integration work.
## Why It Matters
Developer Tools matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- developer tools
- generators
- inspectors
- visual editors
- debug workflows
## Typical Workflow
1. Choose the tool that matches the setup or debugging task.
2. Review generated code or detected state before applying it to the host project.
3. Use tool output as a starting point, then validate it against the application's real configuration.
## Detailed Behavior
Developer Tools works as a product workflow with three layers. The first layer is the user-facing experience: what an operator sees, what they can search or inspect, and what actions they can take. The second layer is the domain model: which records, relationships, or configuration values the workflow depends on. The third layer is the safety model: which permissions, access rules, or validation steps prevent accidental or unauthorized changes.

For Better Auth Studio, this feature is easiest to understand from the operator's point of view. A reader can understand what problem this page solves, what state it reads or changes, and what a successful outcome looks like before they need to think about source files.

## Common Scenarios
- A new operator wants to understand what developer tools is for before using it in production.
- A developer is wiring the feature into a host application and needs to know which data, config, or permissions are involved.
- A maintainer changed the feature and needs to update docs without forcing readers to understand the source tree first.

## Success Criteria
- Readers can explain what developer tools does in plain language.
- Readers know the main objects or settings involved before they see any file paths.
- Readers know which actions are safe, which actions are destructive, and which actions depend on access control.
## How It Fits in the Product
Developer Tools is a product capability, not just a list of source files. The important story is the user-facing outcome, the data or configuration it depends on, and the guardrails that make the workflow safe to operate.
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Other Commands
```bash
# Check version
pnpx better-auth-studio --version

# Show help
pnpx better-auth-studio --help
```
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- User-facing state, available actions, empty/error states, and permission boundaries are part of the product behavior.
- The page explains what data the feature reads or changes before referencing source files.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `./dist/cli.js` - CLI entrypoint or command implementation that defines developer-facing commands and flags.
- `frontend/src/components/CodeEditor.tsx` - CodeEditor reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/CommandPalette.tsx` - CommandPalette reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/lib/studio-tools.ts` - Evidence file that contributes to the implementation understanding for this page.
- `frontend/src/pages/DatabaseVisualizer.tsx` - DatabaseVisualizer page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/EmailEditor.tsx` - EmailEditor page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/Tools.tsx` - Tools page-level UI. This is where users usually encounter the capability in the product.
- `src/cli.ts` - CLI entrypoint or command implementation that defines developer-facing commands and flags.

### Knowledge Chunks
- `README.md#docker-standalone-self-host:1-53` - Docker (Standalone Self Host) (readme; Markdown; heading README > Docker (Standalone Self Host) > Docker (Standalone Self Host)). If you want to run Better Auth Studio in its own container, this repository includes a Docker setup for the standalone CLI server. This mode is useful when: you want a separate admin container your Better Auth app already lives in its own repository you want Docker managed node modules and package manager cache volumes Important: The standalone container still needs your Better Auth project mounted into it, because S
- `README.md#installation-for-self-hosting:1-7` - Installation for Self Hosting (readme; Markdown; heading README > Installation for Self Hosting > Installation for Self Hosting). Important: For self hosting, install better auth studio as a regular dependency (not devDependency) since it's required at runtime in production: Note: The CLI usage (standalone studio) can be installed as a dev dependency, but self hosting requires it in dependencies for production deployments.
- `README.md#next-js-app-router:1-12` - Next.js (App Router) (readme; Markdown; heading README > Next.js (App Router) > Next.js (App Router)). The init command automatically creates app/api/studio/[[...path]]/route.ts: Access at http://localhost:3000/api/studio
- `frontend/src/components/CodeEditor.tsx:1-51` - CodeEditor (source; TypeScript; module frontend/src; symbols CodeEditor). import { useEffect, useRef, useState } from "react"; import { Prism as SyntaxHighlighter } from "react syntax highlighter"; import { oneLight, vscDarkPlus } from "react syntax highlighter/dist/esm/styles/prism"; import { useTheme } from "../contexts/ThemeContext"; interface CodeEditorProps { value: string; onChange: (value: string) = void; language?: string; placeholder?: string; className?: string; } export function
- `frontend/src/components/CodeEditor.tsx:51-95` - CodeEditor (source; TypeScript; module frontend/src; symbols CodeEditor). 'ui monospace, SFMono Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace'; const palette = theme === "light" ? oneLight : vscDarkPlus; const caretColor = theme === "light" ? " 111827" : " ffffff"; // Custom style for syntax highlighter const customStyle = { ...palette, 'pre[class ="language "]': { ...palette['pre[class ="language "]'], background: "transparent", padding: 0, margin: 0, borderRadius: 0,
- `frontend/src/components/CodeEditor.tsx:95-161` - CodeEditor (source; TypeScript; module frontend/src; symbols CodeEditor). className={relative border border dashed border white/20 rounded none focus within:border white/40 ${className}} {/ Syntax highlighted background /} ( {children} )} {highlightCode} {/ Editable textarea /} onChange(e.target.value)} placeholder={placeholder} className="relative w full min h [200px] bg transparent text white font mono text xs rounded none focus:outline none resize none" style={{ ...textareaStyle, boxSiz
- `frontend/src/components/CommandPalette.tsx:1-51` - CommandPalette (source; TypeScript; module frontend/src; symbols CommandPalette). import { Command } from "cmdk"; import type { LucideIcon } from "lucide react"; import { ArrowRight, BarChart3, Building2, Database, Download, ExternalLink, Globe, Key, Lock, Mail, Moon, // Monitor, Plus, RefreshCw, Search, Settings, Share2, Shield, Shuffle, Sun, UserPlus, Users, Wrench, } from "lucide react"; import { useEffect, useState } from "react"; import { useNavigate } from "react router dom"; import { useThe
- `frontend/src/components/CommandPalette.tsx:51-102` - CommandPalette (source; TypeScript; module frontend/src; symbols CommandPalette). export default function CommandPalette({ isOpen, onClose, onAction }: CommandPaletteProps) { const navigate = useNavigate(); const { theme, toggleTheme } = useTheme(); const [search, setSearch] = useState(""); const [plugins, setPlugins] = useState (null); useEffect(() = { const fetchPlugins = async () = { try { const response = await fetch("/api/plugins"); const data = await response.json(); setPlugins(data); } catc
- `frontend/src/components/CommandPalette.tsx:102-166` - CommandPalette (source; TypeScript; module frontend/src; symbols CommandPalette). led("organization"), disabledMessage: "Enable organization plugin in settings", }, { id: "sessions", title: "Sessions", description: "View active user sessions", icon: Globe, action: () = navigate("/sessions"), category: "Navigation", keywords: ["session", "login", "active", "token"], }, { id: "emails", title: "Emails", description: "View email templates", icon: Mail, action: () = navigate("/emails"), category: "Navi
- `frontend/src/components/CommandPalette.tsx:166-219` - CommandPalette (source; TypeScript; module frontend/src; symbols CommandPalette). gate("/organizations", { state: { openModal: "create" } }), category: "Actions", keywords: ["add", "new", "org", "organization", "workspace"], requiresPlugin: "organization", disabled: !isPluginEnabled("organization"), disabledMessage: "Enable organization plugin in settings", }, // { // id: "create session", // title: "Create Session", // description: "Create a new user session", // icon: Monitor, // action: () = na

### Source Details
- `./dist/cli.js`: language JavaScript; flags `--api-dir`, `--config`, `--geo-db`, `--host`, `--no-open`, `--port`
- `frontend/src/components/CodeEditor.tsx`: language TypeScript; exports `CodeEditor`
- `frontend/src/components/CommandPalette.tsx`: language TypeScript; exports `CommandPalette`
- `frontend/src/lib/studio-tools.ts`: language TypeScript; exports `ALL_STUDIO_TOOL_IDS`, `getExcludedToolIds`, `getVisibleToolsCount`, `StudioToolsConfig`
- `frontend/src/pages/DatabaseVisualizer.tsx`: language TypeScript; exports `DatabaseVisualizer`
- `frontend/src/pages/EmailEditor.tsx`: language TypeScript; exports `EmailEditor`
- `frontend/src/pages/Tools.tsx`: language TypeScript; exports `Tools`
- `src/cli.ts`: language TypeScript; flags `--api-dir`, `--config`, `--geo-db`, `--host`, `--no-open`, `--port`

### Evidence
- `./dist/cli.js`
- `frontend/src/components/CodeEditor.tsx`
- `frontend/src/components/CommandPalette.tsx`
- `frontend/src/lib/studio-tools.ts`
- `frontend/src/pages/DatabaseVisualizer.tsx`
- `frontend/src/pages/EmailEditor.tsx`
- `frontend/src/pages/Tools.tsx`
- `README.md#docker-standalone-self-host`
- `README.md#installation-for-self-hosting`
- `README.md#next-js-app-router`
- `src/cli.ts`
