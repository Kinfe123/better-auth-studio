---
title: "Solid Start"
description: "Solid Start is a repository module detected from 28 source files."
order: 560
---

# Solid Start

Solid Start is a repository module detected from 28 source files.
## What This Module Owns
Solid Start owns the behavior described by the source-backed function docs and implementation facts below. The module key is `examples/solid-start`, and the evidence comes from `examples/solid-start/postcss.config.cjs`, `examples/solid-start/src/components/Counter.tsx`, `examples/solid-start/src/entry-client.tsx`, `examples/solid-start/src/entry-server.tsx`, `examples/solid-start/src/env/client.ts`, `examples/solid-start/src/env/schema.ts`, `examples/solid-start/src/env/server.ts`, `examples/solid-start/src/lib/auth-client.ts`, `examples/solid-start/src/lib/auth.ts`, `examples/solid-start/src/root.tsx`.
## What It Actually Does
The attached source chunks do not include function-level comments, so this page stays conservative and uses the implementation map below as the source of truth.

## Behavior From Source Evidence
- Recognizes evidence markers such as `border-sky-600`, `block`, `basePath`.
- Maps detected inputs to normalized values such as `border-sky-600` -> `border-transparent hover:border-sky-600`, `block` -> `hidden`, `basePath` -> `/api/auth`.
## Module Responsibilities
- authClient
- clientEnv
- clientScheme
- Counter
- Examples
- formatErrors
- serverEnv
- serverScheme
- Solid Start
## Public or Observable Surface
The items below are extracted from the files attached to this page. Treat them as evidence for what the module exposes or handles, not as a guarantee that every symbol is public API.
- `auth`
- `authClient`
- `clientEnv`
- `clientScheme`
- `Counter`
- `formatErrors`
- `Root`
- `serverEnv`
- `serverScheme`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Start Studio
```bash
pnpx better-auth-studio start [options]
```

**Options:**

- `--port <number>` - Specify port (default: 3002)
- `--host <string>` - Specify host (default: localhost)
- `--no-open` - Don't automatically open browser
- `--config <path>` - Path to auth config file (default: auto-detect)
- `--watch` - Watch for changes in auth config file and reload server automatically

`3002` is just the standalone Studio default so it does not clash with apps that already use `3000`. You can run Studio on `3000`, `4000`, or any other free port.

**Examples:**

```bash
# Start on custom port (if installed as dev dependency)
pnpm better-auth-studio start --port 3001

# Or with pnpx
pnpx better-auth-studio start --port 3001

# Start without opening browser
pnpm better-auth-studio start --no-open

# Use custom config file
pnpm better-auth-studio start --config ./custom-auth.ts

# Enable watch mode for auto-reload on config changes
pnpm better-auth-studio start --watch

# Combine multiple options
pnpx better-auth-studio start --port 3001 --watch --config ./src/auth.ts
```
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/solid-start/postcss.config.cjs` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/components/Counter.tsx` - Counter reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/solid-start/src/entry-client.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/entry-server.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/client.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/schema.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/server.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/lib/auth-client.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/lib/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `examples/solid-start/src/root.tsx` - Example project file that shows how the capability is wired in a real framework setup.

### Knowledge Chunks
- `examples/solid-start/postcss.config.cjs:1-7` - Solid Start (example; javascript; module examples/solid-start). module.exports = { plugins: { tailwindcss: {}, autoprefixer: {}, }, };
- `examples/solid-start/src/components/Counter.tsx:1-14` - Counter (example; TypeScript; module examples/solid-start; symbols Counter). import { createSignal } from "solid js"; export default function Counter() { const [count, setCount] = createSignal(0); return ( setCount(count() + 1)} Clicks: {count()} ); }
- `examples/solid-start/src/entry-client.tsx:1-4` - Solid Start (example; tsx; module examples/solid-start). import { mount, StartClient } from "solid start/entry client"; mount(() = , document);
- `examples/solid-start/src/entry-server.tsx:1-4` - Solid Start (example; tsx; module examples/solid-start). import { StartServer, createHandler, renderAsync } from "solid start/entry server"; export default createHandler(renderAsync((event) = ));
- `examples/solid-start/src/env/client.ts:1-19` - clientEnv, formatErrors (example; TypeScript; module examples/solid-start; symbols clientEnv, formatErrors). import type { ZodFormattedError } from "zod"; import { clientScheme } from "./schema"; export const formatErrors = (errors: ZodFormattedError , string ) = Object.entries(errors) .map(([name, value]) = { if (value && " errors" in value) return ${name}: ${value. errors.join(", ")}\n; }) .filter(Boolean); const env = clientScheme.safeParse(import.meta.env); if (env.success === false) { console.error("❌ Invalid environme
- `examples/solid-start/src/env/schema.ts:1-26` - clientScheme, serverScheme (example; TypeScript; module examples/solid-start; symbols clientScheme, serverScheme). import { isServer } from "solid js/web"; import { z } from "zod"; export const serverScheme = z.object({ NODE ENV: z.enum(["development", "production", "test"]).default("development"), AUTH SECRET: z.string(), DATABASE URL: z.string(), START BASE URL: z.preprocess( // This makes Vercel deployments not fail if you don't set START BASE URL // Since NextAuth.js automatically uses the VERCEL URL if present. (str) = proce
- `examples/solid-start/src/env/server.ts:1-19` - formatErrors, serverEnv (example; TypeScript; module examples/solid-start; symbols formatErrors, serverEnv). import { serverScheme } from "./schema"; import type { ZodFormattedError } from "zod"; export const formatErrors = (errors: ZodFormattedError , string ) = Object.entries(errors) .map(([name, value]) = { if (value && " errors" in value) return ${name}: ${value. errors.join(", ")}\n; }) .filter(Boolean); const env = serverScheme.safeParse(process.env); if (env.success === false) { console.error("❌ Invalid environment v
- `examples/solid-start/src/lib/auth-client.ts:1-7` - authClient (example; TypeScript; module examples/solid-start; symbols authClient). import { createAuthClient } from "better auth/solid"; import { clientEnv } from "~/env/client"; export const authClient = createAuthClient({ baseURL: clientEnv.START BASE URL, });
- `examples/solid-start/src/lib/auth.ts:1-59` - auth (example; TypeScript; module examples/solid-start; symbols auth). import { betterAuth } from "better auth"; import { prismaAdapter } from "better auth/adapters/prisma"; import { organization, admin } from "better auth/plugins"; import { prisma } from "~/server/db/client"; import { serverEnv } from "~/env/server"; export const auth = betterAuth({ secret: serverEnv.AUTH SECRET, database: prismaAdapter(prisma, { provider: "postgresql" }), baseURL: serverEnv.BETTER AUTH URL || serverEn
- `examples/solid-start/src/root.tsx:1-62` - Root (example; TypeScript; module examples/solid-start; symbols Root). // @refresh reload import { Suspense } from "solid js"; import { useLocation, A, Body, ErrorBoundary, FileRoutes, Head, Html, Meta, Routes, Scripts, Title, } from "solid start"; import "./root.css"; import { session } from "./utils/auth"; import { authClient } from "./lib/auth client"; export default function Root() { const location = useLocation(); const active = (path: string) = path == location.pathname ? "border

### Source Details
- `examples/solid-start/src/components/Counter.tsx`: language TypeScript; exports `Counter`
- `examples/solid-start/src/env/client.ts`: language TypeScript; exports `clientEnv`, `formatErrors`
- `examples/solid-start/src/env/schema.ts`: language TypeScript; exports `clientScheme`, `serverScheme`; env `VERCEL`, `VERCEL_URL`
- `examples/solid-start/src/env/server.ts`: language TypeScript; exports `formatErrors`, `serverEnv`
- `examples/solid-start/src/lib/auth-client.ts`: language TypeScript; exports `authClient`
- `examples/solid-start/src/lib/auth.ts`: language TypeScript; exports `auth`
- `examples/solid-start/src/root.tsx`: language TypeScript; exports `Root`

### Evidence
- `examples/solid-start/postcss.config.cjs`
- `examples/solid-start/src/components/Counter.tsx`
- `examples/solid-start/src/entry-client.tsx`
- `examples/solid-start/src/entry-server.tsx`
- `examples/solid-start/src/env/client.ts`
- `examples/solid-start/src/env/schema.ts`
- `examples/solid-start/src/env/server.ts`
- `examples/solid-start/src/lib/auth-client.ts`
- `examples/solid-start/src/lib/auth.ts`
- `examples/solid-start/src/root.tsx`
