---
title: "Tanstack Start"
description: "Tanstack Start is a repository module detected from 22 source files."
order: 580
---

# Tanstack Start

Tanstack Start is a repository module detected from 22 source files.
## What This Module Owns
Tanstack Start owns a coherent part of the codebase. This page is generated from module structure, public symbols, routes, and package exports so larger repositories produce deeper docs instead of a fixed sidebar.
The module key is `examples/tanstack-start`. It is included because source files, declarations, routes, or package exports show this area has behavior worth documenting.
## Module Responsibilities
- accounts
- Examples
- Header
- MetaTheme
- ModeToggle
- Providers
- sessions
- Tanstack Start
- users
- verifications
## Detailed Module Model
Tanstack Start is generated from a coherent source area, not from a generic keyword. The docs engine groups files into this page when they share a module boundary, package export, route family, or public symbol surface.

For Better Auth Studio, module pages help the docs grow with the repository. Small codebases may only need a few module pages; larger codebases can expose many focused pages so readers can learn one capability, adapter, plugin, or subsystem at a time.

## What to Look For
- The responsibility owned by this module.
- The public exports, route handlers, declarations, or configuration values attached to the module.
- The neighboring docs pages that explain related user-facing workflows or integration details.

## Success Criteria
- Readers understand why this source area exists before reading file paths.
- Readers can find the files that define the module without searching the whole repo.
- Maintainers can update this page when the module's exports, routes, or responsibilities change.
## Public or Observable Surface
The items below are extracted from the files attached to this page. Treat them as evidence for what the module exposes or handles, not as a guarantee that every symbol is public API.
- `accounts`
- `authClient`
- `Button`
- `buttonVariants`
- `db`
- `DropdownMenu`
- `DropdownMenuCheckboxItem`
- `DropdownMenuContent`
- `DropdownMenuGroup`
- `DropdownMenuItem`
- `DropdownMenuLabel`
- `DropdownMenuPortal`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Start Studio
```bash
pnpx better-auth-studio start [options]
```

**Options:**

- `--port <number>` - Specify port (default: 3002)
- `--host <string>` - Specify host (default: localhost)
- `--no-open` - Don't automatically open browser
- `--config <path>` - Path to auth config file (default: auto-detect)
- `--watch` - Watch for changes in auth config file and reload server automatically

`3002` is just the standalone Studio default so it does not clash with apps that already use `3000`. You can run Studio on `3000`, `4000`, or any other free port.

**Examples:**

```bash
# Start on custom port (if installed as dev dependency)
pnpm better-auth-studio start --port 3001

# Or with pnpx
pnpx better-auth-studio start --port 3001

# Start without opening browser
pnpm better-auth-studio start --no-open

# Use custom config file
pnpm better-auth-studio start --config ./custom-auth.ts

# Enable watch mode for auto-reload on config changes
pnpm better-auth-studio start --watch

# Combine multiple options
pnpx better-auth-studio start --port 3001 --watch --config ./src/auth.ts
```
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Module pages are generated only from concrete source, route, declaration, or package export evidence.
- If the module is internal, keep the page focused on responsibility and maintenance boundaries rather than promising public APIs.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/tanstack-start/drizzle/auth-schema.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/tanstack-start/drizzle/src/components/header.tsx` - Header reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/tanstack-start/drizzle/src/components/meta-theme.ts` - Meta Theme reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/tanstack-start/drizzle/src/components/mode-toggle.tsx` - Mode Toggle reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/tanstack-start/drizzle/src/components/providers.tsx` - Providers reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/tanstack-start/drizzle/src/components/ui/button.tsx` - Button reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/tanstack-start/drizzle/src/components/ui/dropdown-menu.tsx` - Dropdown Menu reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/tanstack-start/drizzle/src/database/db.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/tanstack-start/drizzle/src/database/schema.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/tanstack-start/drizzle/src/lib/auth-client.ts` - Example project file that shows how the capability is wired in a real framework setup.

### Knowledge Chunks
No semantic knowledge chunks were generated directly for these evidence files.

### Source Details
- `examples/tanstack-start/drizzle/auth-schema.ts`: language TypeScript; exports `accounts`, `sessions`, `users`, `verifications`
- `examples/tanstack-start/drizzle/src/components/header.tsx`: language TypeScript; exports `Header`
- `examples/tanstack-start/drizzle/src/components/meta-theme.ts`: language TypeScript; exports `MetaTheme`
- `examples/tanstack-start/drizzle/src/components/mode-toggle.tsx`: language TypeScript; exports `ModeToggle`
- `examples/tanstack-start/drizzle/src/components/providers.tsx`: language TypeScript; exports `Providers`
- `examples/tanstack-start/drizzle/src/components/ui/button.tsx`: language TypeScript; exports `Button`, `buttonVariants`
- `examples/tanstack-start/drizzle/src/components/ui/dropdown-menu.tsx`: language TypeScript; exports `DropdownMenu`, `DropdownMenuCheckboxItem`, `DropdownMenuContent`, `DropdownMenuGroup`, `DropdownMenuItem`, `DropdownMenuLabel`
- `examples/tanstack-start/drizzle/src/database/db.ts`: language TypeScript; exports `db`; env `DATABASE_URL`

### Evidence
- `examples/tanstack-start/drizzle/auth-schema.ts`
- `examples/tanstack-start/drizzle/src/components/header.tsx`
- `examples/tanstack-start/drizzle/src/components/meta-theme.ts`
- `examples/tanstack-start/drizzle/src/components/mode-toggle.tsx`
- `examples/tanstack-start/drizzle/src/components/providers.tsx`
- `examples/tanstack-start/drizzle/src/components/ui/button.tsx`
- `examples/tanstack-start/drizzle/src/components/ui/dropdown-menu.tsx`
- `examples/tanstack-start/drizzle/src/database/db.ts`
- `examples/tanstack-start/drizzle/src/database/schema.ts`
- `examples/tanstack-start/drizzle/src/lib/auth-client.ts`
