---
title: "Database Detection"
description: "Database Detection is a repository module detected from 1 source file."
order: 930
---

# Database Detection

Database Detection is a repository module detected from 1 source file.
## What This Module Owns
Database Detection owns the behavior described by the source-backed function docs and implementation facts below. The module key is `database-detection`, and the evidence comes from `src/utils/database-detection.ts`.
## What It Actually Does
- `detectDatabase(cwd?: string): Promise<DetectionInfo | undefined>` - Detect database type and version from installed packages Parameters: cwd - Current working directory to search in. Returns: DetectionInfo with database name and version, or undefined if not found.
- `detectDatabaseWithDialect(cwd?: string): Promise<DatabaseDetectionResult | undefined>` - Enhanced database detection with dialect information Parameters: cwd - Current working directory to search in. Returns: DatabaseDetectionResult with database name, version, and dialect info.

## Behavior From Source Evidence
- Recognizes evidence markers such as `drizzle-orm`, `@prisma/client`, `better-sqlite3`, `mongoose`, `mongodb`, `pg`, `mysql`, `mariadb`, `sqlite3`.
- Maps detected inputs to normalized values such as `drizzle-orm` -> `drizzle`, `@prisma/client` -> `prisma`, `better-sqlite3` -> `sqlite`, `mongoose` -> `mongodb`, `mongodb` -> `mongodb`, `pg` -> `postgresql`, `mysql` -> `mysql`, `mariadb` -> `mariadb`, and more.
## Module Responsibilities
- Database Detection
- detectAllDatabases
- detectDatabase
- detectDatabaseWithDialect
## Public or Observable Surface
The items below are extracted from the files attached to this page. Treat them as evidence for what the module exposes or handles, not as a guarantee that every symbol is public API.
- `detectAllDatabases`
- `detectDatabase`
- `detectDatabaseWithDialect`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `src/utils/database-detection.ts` - Database Detection utility. It supports the feature behind the scenes and is often where shared rules live.

### Knowledge Chunks
- `src/utils/database-detection.ts:1-64` - detectAllDatabases, detectDatabase, detectDatabaseWithDialect (source; TypeScript; module database-detection; symbols detectAllDatabases, detectDatabase, detectDatabaseWithDialect). import type { DatabaseDetectionResult, DetectionInfo } from "../types"; import { getPackageVersion } from "./package json.js"; const DATABASES: Record = { "drizzle orm": "drizzle", "@prisma/client": "prisma", mongoose: "mongodb", mongodb: "mongodb", pg: "postgresql", mysql: "mysql", mariadb: "mariadb", sqlite3: "sqlite", "better sqlite3": "sqlite", }; const DATABASE DIALECTS: Record = { postgresql: ["pg", "postgres"]
- `src/utils/database-detection.ts:64-142` - detectAllDatabases, detectDatabase, detectDatabaseWithDialect (source; TypeScript; module database-detection; symbols detectAllDatabases, detectDatabase, detectDatabaseWithDialect). const betterSqlite3Version = await getPackageVersion("better sqlite3", cwd); if (betterSqlite3Version) { adapter = "better sqlite3"; } else if (sqlite3Version) { adapter = "sqlite3"; } break; } case "prisma": { const prismaDialect = await detectPrismaDialect(cwd); if (prismaDialect) { dialect = prismaDialect; } adapter = "prisma"; break; } case "drizzle": { const drizzleDialect = await detectDrizzleDialect(cwd); if (
- `src/utils/database-detection.ts:142-157` - detectAllDatabases, detectDatabase, detectDatabaseWithDialect (source; TypeScript; module database-detection; symbols detectAllDatabases, detectDatabase, detectDatabaseWithDialect). export async function detectAllDatabases(cwd?: string): Promise { const results: DatabaseDetectionResult[] = []; for (const [pkg, name] of Object.entries(DATABASES)) { const version = await getPackageVersion(pkg, cwd); if (version) { const detection = await detectDatabaseWithDialect(cwd); if (detection && detection.name === name) { results.push(detection); } } } return results; }

### Source Details
- `src/utils/database-detection.ts`: language TypeScript; exports `detectAllDatabases`, `detectDatabase`, `detectDatabaseWithDialect`

### Evidence
- `src/utils/database-detection.ts`
