---
title: "Handlers"
description: "Handlers is a repository module detected from 1 source file."
order: 1080
---

# Handlers

Handlers is a repository module detected from 1 source file.
## What This Module Owns
Handlers owns the behavior described by the source-backed function docs and implementation facts below. The module key is `core/handlers`, and the evidence comes from `src/core/handler.ts`.
## What It Actually Does
- `handleStudioRequest(request: UniversalRequest, config: StudioConfig): Promise<UniversalResponse>` - Main handler - processes all studio requests (framework-agnostic)

## Behavior From Source Evidence
- Recognizes evidence markers such as `Content-Type`, `error`.
- Maps detected inputs to normalized values such as `Content-Type` -> `application/json`, `error` -> `Internal server error`.
## Module Responsibilities
- Core
- getAuthAdapter
- Handlers
- handleStudioRequest
- initializeEventIngestionAndHooks
## Public or Observable Surface
The items below are extracted from the files attached to this page. Treat them as evidence for what the module exposes or handles, not as a guarantee that every symbol is public API.
- `getAuthAdapter`
- `handleStudioRequest`
- `initializeEventIngestionAndHooks`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `src/core/handler.ts` - Evidence file that contributes to the implementation understanding for this page.

### Knowledge Chunks
- `src/core/handler.ts:1-39` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). import { existsSync, readdirSync, readFileSync, realpathSync, statSync } from "fs"; import { dirname, extname, join, resolve } from "path"; import { fileURLToPath } from "url"; import { createClickHouseProvider, createHttpProvider, createNodeSqliteProvider, createPostgresProvider, createSqliteProvider, createStorageProvider, } from "../providers/events/helpers.js"; import type { EventIngestionProvider } from "../type
- `src/core/handler.ts:39-119` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). export async function initializeEventIngestionAndHooks(config: StudioConfig): Promise { if (config.events?.enabled && !isEventIngestionInitialized()) { let provider: EventIngestionProvider | undefined; if ( config.events.provider && typeof config.events.provider === "object" && typeof config.events.provider.ingest === "function" ) { provider = config.events.provider; } else if (config.events.client && config.events.c
- `src/core/handler.ts:119-139` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). auth, config); } } / Main handler processes all studio requests (framework agnostic) / export async function handleStudioRequest( request: UniversalRequest, config: StudioConfig, ): Promise { try { const isSelfHosted = !!config.basePath; const basePath = config.basePath || ""; let path = request.url; const [pathname, queryString] = path.split("?"); if (isSelfHosted && basePath) {
- `src/core/handler.ts:139-201` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). const normalizedBasePath = basePath.endsWith("/") ? basePath.slice(0, 1) : basePath; let normalizedPath = pathname; if (normalizedPath === normalizedBasePath || normalizedPath === normalizedBasePath + "/") { normalizedPath = "/"; } else if (normalizedPath.startsWith(normalizedBasePath + "/")) { normalizedPath = normalizedPath.slice(normalizedBasePath.length); } else if (normalizedPath.startsWith(normalizedBasePath))
- `src/core/handler.ts:201-282` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). return jsonResponse(401, { error: "Unauthorized", message: sessionResult.error }); } } return await handleApiRoute(request, path, config); } if (isSelfHosted && (path === "/auth" || path.startsWith("/auth/"))) { const apiPath = "/api" + path; if (isProtectedApiPath(apiPath)) { const sessionResult = verifyStudioSession(request, config); if (!sessionResult.valid) { return jsonResponse(401, { error: "Unauthorized", mess
- `src/core/handler.ts:282-355` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). function parseCookies(cookieHeader: string | undefined): Record { if (!cookieHeader) return {}; const cookies: Record = {}; cookieHeader.split(";").forEach((part) = { const [name, ...rest] = part.trim().split("="); if (name) { cookies[name] = rest.join("="); } }); return cookies; } function verifyStudioSession( request: UniversalRequest, config: StudioConfig, ): { valid: boolean; session?: any; error?: string } { con
- `src/core/handler.ts:355-430` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). if (c.options.sameSite) cookie += ; SameSite=${c.options.sameSite}; if (c.options.maxAge !== undefined) cookie += ; Max Age=${Math.floor(c.options.maxAge / 1000)}; if (c.options.path) cookie += ; Path=${c.options.path}; return cookie; }), ); } return { status: result.status, headers, ...(setCookies.length 0 ? { setCookies } : {}), body: JSON.stringify(result.data), }; } catch (error) { console.error("API route error:
- `src/core/handler.ts:430-478` - getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks (source; TypeScript; module core/handlers; symbols getAuthAdapter, handleStudioRequest, initializeEventIngestionAndHooks). const nmPath = join(searchDir, "node modules", "better auth studio", "dist", "public"); if (existsSync(nmPath)) { candidates.unshift(nmPath); break; } const parent = resolve(searchDir, ".."); if (parent === searchDir) break; searchDir = parent; } } // PRIORITY 4: Resolve from import.meta.url (for ESM) try { const moduleUrl = import.meta.url; if (moduleUrl) { const modulePath = fileURLToPath(moduleUrl); const moduleDi

### Source Details
- `src/core/handler.ts`: language TypeScript; exports `getAuthAdapter`, `handleStudioRequest`, `initializeEventIngestionAndHooks`; env `BETTER_AUTH_SECRET`, `NODE_ENV`, `VERCEL`

### Evidence
- `src/core/handler.ts`
