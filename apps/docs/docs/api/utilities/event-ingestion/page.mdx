---
title: "Event Ingestion"
description: "The source includes a event ingestion utility."
order: 850
---

# Event Ingestion

The source includes a event ingestion utility.
## What This Is
Event Ingestion is documented from the source evidence attached to this page. The goal is to explain what a developer can rely on, what the code detects or returns, and what behavior the utility exposes before listing raw file paths.

## Why It Matters
Event Ingestion matters when a reader needs to understand the behavior exposed by Better Auth Studio without reverse-engineering the implementation first.
## What It Actually Does
- `emitEvent(type: AuthEventType, data: { status: "success" | "failed"; userId?: string; sessionId?: string; organizationId?: string; metadata?: Record<string, any>; request?: { headers: Record<string, string>; ip?: string }; }, eventsConfig?: StudioConfig["events"]): Promise<void>` - Emit an event
- `checkEventIngestionHealth(): Promise<boolean>` - Health check
- `isEventIngestionInitialized(): boolean` - Get initialization status
- `getEventQueueSize(): number` - Get current queue size (for monitoring)
- `getEventIngestionProvider(): EventIngestionProvider | null` - Get the current event ingestion provider

## Behavior From Source Evidence
- Recognizes evidence markers such as `source`.
- Maps detected inputs to normalized values such as `source` -> `app`.
## Import Surface
No package export was detected for this utility page. Treat it as internal support behavior unless package metadata explicitly exposes it as public API.
## API Mental Model
Use the source-backed behavior above as the contract. The related exports below identify the functions or types a maintainer should keep aligned with the docs.
## Related Exports
- `checkEventIngestionHealth`
- `emitEvent`
- `getEventIngestionProvider`
- `getEventQueueSize`
- `initializeEventIngestion`
- `isEventIngestionInitialized`
- `shutdownEventIngestion`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `src/utils/event-ingestion.ts` - Event Ingestion utility. It supports the feature behind the scenes and is often where shared rules live.

### Knowledge Chunks
- `src/utils/event-ingestion.ts:1-28` - checkEventIngestionHealth, emitEvent, getEventIngestionProvider (source; TypeScript; module event-ingestion; symbols checkEventIngestionHealth, emitEvent, getEventIngestionProvider, getEventQueueSize). import { createClickHouseProvider, createHttpProvider, createNodeSqliteProvider, createPostgresProvider, createSqliteProvider, } from "../providers/events/helpers.js"; import type { AuthEvent, AuthEventType, EventIngestionProvider, EventQueryOptions, EventQueryResult, } from "../types/events.js"; import { EVENT TEMPLATES, getEventSeverity } from "../types/events.js"; import type { StudioConfig } from "../types/handle
- `src/utils/event-ingestion.ts:28-120` - checkEventIngestionHealth, emitEvent, getEventIngestionProvider (source; TypeScript; module event-ingestion; symbols checkEventIngestionHealth, emitEvent, getEventIngestionProvider, getEventQueueSize). export function initializeEventIngestion(eventsConfig: StudioConfig["events"]): void { if (!eventsConfig?.enabled) { return; } if (isInitialized) { return; } config = eventsConfig; if (eventsConfig.provider) { provider = eventsConfig.provider; } else if (eventsConfig.client && eventsConfig.clientType) { switch (eventsConfig.clientType) { case "postgres": case "prisma": case "drizzle": try { provider = createPostgresP
- `src/utils/event-ingestion.ts:120-168` - checkEventIngestionHealth, emitEvent, getEventIngestionProvider (source; TypeScript; module event-ingestion; symbols checkEventIngestionHealth, emitEvent, getEventIngestionProvider, getEventQueueSize). enabled) { initializeEventIngestion(eventsConfig); } const activeConfig = eventsConfig || config; if (!activeConfig?.enabled) { return; } const useConfig = activeConfig || config; if (!useConfig) { return; } if (!provider) { return; } if (useConfig.include && !useConfig.include.includes(type)) { return; } if (useConfig.exclude && useConfig.exclude.includes(type)) { return; } const template = EVENT TEMPLATES[type]; co
- `src/utils/event-ingestion.ts:168-269` - checkEventIngestionHealth, emitEvent, getEventIngestionProvider (source; TypeScript; module event-ingestion; symbols checkEventIngestionHealth, emitEvent, getEventIngestionProvider, getEventQueueSize). userAgent: data.request?.headers["user agent"] || data.request?.headers["User Agent"], source: "app", display: { message: displayMessage, severity: getEventSeverity(tempEvent, data.status), }, }; // Call onEventIngest callback if provided if (useConfig.onEventIngest) { try { await useConfig.onEventIngest(event); } catch (error) { // Don't block event ingestion if callback fails console.error("onEventIngest callback e
- `src/utils/event-ingestion.ts:269-295` - checkEventIngestionHealth, emitEvent, getEventIngestionProvider (source; TypeScript; module event-ingestion; symbols checkEventIngestionHealth, emitEvent, getEventIngestionProvider, getEventQueueSize). healthCheck(); } return true; } / Get initialization status / export function isEventIngestionInitialized(): boolean { return isInitialized; } / Get current queue size (for monitoring) / export function getEventQueueSize(): number { return eventQueue.length; } / Get the current event ingestion provider / export function getEventIngestionProvider(): EventIngestionProvider | null { return provider; }

### Source Details
- `src/utils/event-ingestion.ts`: language TypeScript; exports `checkEventIngestionHealth`, `emitEvent`, `getEventIngestionProvider`, `getEventQueueSize`, `initializeEventIngestion`, `isEventIngestionInitialized`

### Evidence
- `src/utils/event-ingestion.ts`
