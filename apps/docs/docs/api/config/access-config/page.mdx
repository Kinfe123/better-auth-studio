---
title: "Access Config"
description: "The source includes types or helpers for access config."
order: 780
---

# Access Config

The source includes types or helpers for access config.
## What This Is
Access Config is documented from the source evidence attached to this page. The goal is to explain what a developer can rely on, what the code detects or returns, and what behavior the utility exposes before listing raw file paths.

## Why It Matters
Access Config matters when a reader needs to understand the behavior exposed by Better Auth Studio without reverse-engineering the implementation first.
## What It Actually Does
The attached source chunks do not include function-level comments, so this page stays conservative and uses the implementation map below as the source of truth.

## Behavior From Source Evidence
- Recognizes evidence markers such as `x-forwarded-for`, `reason`, `message`, `path`, `method`.
- Maps detected inputs to normalized values such as `x-forwarded-for` -> `203.0.113.2, 10.0.0.5`, `x-forwarded-for` -> `203.0.113.2`, `x-forwarded-for` -> `198.51.100.8`, `x-forwarded-for` -> `10.0.1.15`, `reason` -> `ip_not_allowed`, `message` -> `Access denied. This IP address is not in the allowed list.`, `reason` -> `ip_blocked`, `message` -> `Access denied. This IP address is blocked.`, and more.
## Import Surface
Use `better-auth-studio` as the package path indicated by package metadata or adapter file evidence.
## API Mental Model
Use the source-backed behavior above as the contract. The related exports below identify the functions or types a maintainer should keep aligned with the docs.
## Related Exports
- `AccessDeniedPage`
- `AccessEvaluationInput`
- `AccessEvaluationResult`
- `evaluateRequestAccess`
- `extractClientIp`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Configuration Options
| Option                    | Required | Description                                                                                                                                   |
| ------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth`                    | Yes      | Your Better Auth instance                                                                                                                     |
| `basePath`                | Yes      | URL path where studio is mounted                                                                                                              |
| `access.allowEmails`      | No       | Array of admin email addresses                                                                                                                |
| `access.roles`            | No       | Array of allowed user roles                                                                                                                   |
| `access.allowIpAddresses` | No       | IP allowlist for Studio requests. Supports exact IPs and wildcard patterns (example: `192.168.*`)                                             |
| `access.blockIpAddresses` | No       | IP blocklist for Studio requests. Supports exact IPs and wildcard patterns                                                                    |
| `ipAddress`               | No       | IP geolocation for Events/Sessions: `provider` ("ipinfo" \| "ipapi"), `apiToken`, `baseUrl`, optional `endpoint` (ipinfo: "lite" \| "lookup") |
| `lastSeenAt`              | No       | Enable last-seen tracking: `{ enabled: true, columnName?: string }`                                                                           |
| `metadata`                | No       | Custom branding (title, theme)                                                                                                                |

### Next.js (App Router)
The init command automatically creates `app/api/studio/[[...path]]/route.ts`:

```typescript
import { betterAuthStudio } from "better-auth-studio/nextjs";
import studioConfig from "@/studio.config";

const handler = betterAuthStudio(studioConfig);

export { handler as GET, handler as POST, handler as PUT, handler as DELETE, handler as PATCH };
```

Access at `http://localhost:3000/api/studio`

### Express
Add the studio handler to your server:

```typescript
import express from "express";
import { toNodeHandler } from "better-auth/node";
import { betterAuthStudio } from "better-auth-studio/express";
import { auth } from "./auth";
import studioConfig from "./studio.config";

const app = express();

app.use(express.json());
app.use("/api/studio", betterAuthStudio(studioConfig));
app.all("/api/auth/*", toNodeHandler(auth));

app.listen(3000);
```

Access at `http://localhost:3000/api/studio`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `frontend/src/pages/AccessDenied.tsx` - AccessDenied page-level UI. This is where users usually encounter the capability in the product.
- `src/utils/access-rules.ts` - Access Rules utility. It supports the feature behind the scenes and is often where shared rules live.
- `tests/access-rules.test.ts` - Evidence file that contributes to the implementation understanding for this page.

### Knowledge Chunks
- `README.md#configuration-options:1-11` - Configuration Options (readme; Markdown; heading README > Configuration Options > Configuration Options). | Option | Required | Description | | | | | | auth | Yes | Your Better Auth instance | | basePath | Yes | URL path where studio is mounted | | access.allowEmails | No | Array of admin email addresses | | access.roles | No | Array of allowed user roles | | access.allowIpAddresses | No | IP allowlist for Studio requests. Supports exact IPs and wildcard patterns (example: 192.168. ) | | access.blockIpAddresses | No | IP
- `README.md#express:1-19` - Express (readme; Markdown; heading README > Express > Express). Add the studio handler to your server: Access at http://localhost:3000/api/studio
- `README.md#next-js-app-router:1-12` - Next.js (App Router) (readme; Markdown; heading README > Next.js (App Router) > Next.js (App Router)). The init command automatically creates app/api/studio/[[...path]]/route.ts: Access at http://localhost:3000/api/studio
- `frontend/src/pages/AccessDenied.tsx:1-52` - AccessDeniedPage (source; TypeScript; module frontend/src; symbols AccessDeniedPage). import { useNavigate } from "react router dom"; import { fetchStudioAuthJson } from "../utils/studio auth"; export default function AccessDeniedPage() { const navigate = useNavigate(); const handleLogout = async () = { try { await fetchStudioAuthJson("/logout"); navigate("/login"); } catch { navigate("/login"); } }; return ( access denied
- `frontend/src/pages/AccessDenied.tsx:52-100` - AccessDeniedPage (source; TypeScript; module frontend/src; symbols AccessDeniedPage). Permission Denied Your account does not have the required role to access Better Auth Studio. Required role: admin Sign Out window.history.back()} className="w full h 10 border border dashed border white/20 text white text xs font mono uppercase tracking wider hover:bg white/5 hover:border white/30 transition colors flex items center justify center gap 2" <path
- `frontend/src/pages/AccessDenied.tsx:100-118` - AccessDeniedPage (source; TypeScript; module frontend/src; symbols AccessDeniedPage). d="M12 4h2v2h 2V4zm 2 2h2v2h 2V6zm 2 2h2v2H8V8zm 2 2h2v4H6v 4zm14 0h2v4h 2v 4zm 2 4h2v2h 2v 2zm 2 2h2v2h 2v 2zm 2 2h2v2h 2v 2z" fill="currentColor" / Go Back Contact your administrator for access ); }
- `src/utils/access-rules.ts:1-68` - AccessEvaluationInput, AccessEvaluationResult, evaluateRequestAccess (source; TypeScript; module access-rules; symbols AccessEvaluationInput, AccessEvaluationResult, evaluateRequestAccess, extractClientIp). import { isIP } from "node:net"; import type { StudioAccessConfig } from "../types/handler.js"; export type AccessEvaluationInput = { accessConfig?: StudioAccessConfig; path: string; method: string; headers: Record ; ip?: string | null; }; export type AccessEvaluationResult = | { allowed: true; ipAddress: string | null; } | { allowed: false; ipAddress: string | null; reason: "ip not allowed" | "ip blocked"; message:
- `src/utils/access-rules.ts:68-141` - AccessEvaluationInput, AccessEvaluationResult, evaluateRequestAccess (source; TypeScript; module access-rules; symbols AccessEvaluationInput, AccessEvaluationResult, evaluateRequestAccess, extractClientIp). function getHeaderValue(headers: Record , headerName: string): string | undefined { const target = headerName.toLowerCase(); for (const [key, value] of Object.entries(headers)) { if (key.toLowerCase() === target) { return value; } } return undefined; } function getForwardedIp(headers: Record ): string | null { const forwarded = getHeaderValue(headers, "forwarded"); if (!forwarded) return null; const entries = forward
- `src/utils/access-rules.ts:141-171` - AccessEvaluationInput, AccessEvaluationResult, evaluateRequestAccess (source; TypeScript; module access-rules; symbols AccessEvaluationInput, AccessEvaluationResult, evaluateRequestAccess, extractClientIp). if (!ipAddress || !allowIpAddresses.some((rule) = ipMatchesRule(ipAddress, rule))) { return { allowed: false, ipAddress, reason: "ip not allowed", message: "Access denied. This IP address is not in the allowed list.", }; } } const blockIpAddresses = accessConfig?.blockIpAddresses?.filter( (value) = value.trim().length 0, ); if (hasRules(blockIpAddresses) && ipAddress) { const blocked = blockIpAddresses.some((rule) =
- `tests/access-rules.test.ts:1-63` - Access Rules.Test 1 (test; typescript). import { describe, expect, it } from "vitest"; import { evaluateRequestAccess, extractClientIp } from "../src/utils/access rules"; describe("access rules", () = { it("extracts IP from x forwarded for", () = { const ip = extractClientIp({ "x forwarded for": "203.0.113.2, 10.0.0.5", }); expect(ip).toBe("203.0.113.2"); }); it("blocks exact IPs from blocklist", () = { const result = evaluateRequestAccess({ path: "/api/au

### Source Details
- `frontend/src/pages/AccessDenied.tsx`: language TypeScript; exports `AccessDeniedPage`
- `src/utils/access-rules.ts`: language TypeScript; exports `AccessEvaluationInput`, `AccessEvaluationResult`, `evaluateRequestAccess`, `extractClientIp`

### Evidence
- `frontend/src/pages/AccessDenied.tsx`
- `README.md#configuration-options`
- `README.md#express`
- `README.md#next-js-app-router`
- `src/utils/access-rules.ts`
- `tests/access-rules.test.ts`
