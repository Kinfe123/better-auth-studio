---
title: "Metadata Config"
description: "The source includes types or helpers for metadata config."
order: 800
---

# Metadata Config

The source includes types or helpers for metadata config.
## What This Is
Metadata Config is documented from the source evidence attached to this page. The goal is to explain what a developer can rely on, what the code detects or returns, and what behavior the utility exposes before listing raw file paths.

## Why It Matters
Metadata Config matters when a reader needs to understand the behavior exposed by Better Auth Studio without reverse-engineering the implementation first.
## What It Actually Does
The attached source chunks do not include function-level comments, so this page stays conservative and uses the implementation map below as the source of truth.

## Behavior From Source Evidence
- Recognizes evidence markers such as `light`.
- Maps detected inputs to normalized values such as `light` -> `dark`.
## Import Surface
Use `better-auth-studio` as the package path indicated by package metadata or adapter file evidence.
## API Mental Model
Use the source-backed behavior above as the contract. The related exports below identify the functions or types a maintainer should keep aligned with the docs.
## Related Exports
- `MetaTheme`
- `StudioTheme`
- `THEME_STORAGE_KEY`
- `ThemeProvider`
- `useTheme`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Configuration Options
| Option                    | Required | Description                                                                                                                                   |
| ------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth`                    | Yes      | Your Better Auth instance                                                                                                                     |
| `basePath`                | Yes      | URL path where studio is mounted                                                                                                              |
| `access.allowEmails`      | No       | Array of admin email addresses                                                                                                                |
| `access.roles`            | No       | Array of allowed user roles                                                                                                                   |
| `access.allowIpAddresses` | No       | IP allowlist for Studio requests. Supports exact IPs and wildcard patterns (example: `192.168.*`)                                             |
| `access.blockIpAddresses` | No       | IP blocklist for Studio requests. Supports exact IPs and wildcard patterns                                                                    |
| `ipAddress`               | No       | IP geolocation for Events/Sessions: `provider` ("ipinfo" \| "ipapi"), `apiToken`, `baseUrl`, optional `endpoint` (ipinfo: "lite" \| "lookup") |
| `lastSeenAt`              | No       | Enable last-seen tracking: `{ enabled: true, columnName?: string }`                                                                           |
| `metadata`                | No       | Custom branding (title, theme)                                                                                                                |
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/tanstack-start/drizzle/src/components/meta-theme.ts` - Meta Theme reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/contexts/ThemeContext.tsx` - Evidence file that contributes to the implementation understanding for this page.

### Knowledge Chunks
- `README.md#configuration-options:1-11` - Configuration Options (readme; Markdown; heading README > Configuration Options > Configuration Options). | Option | Required | Description | | | | | | auth | Yes | Your Better Auth instance | | basePath | Yes | URL path where studio is mounted | | access.allowEmails | No | Array of admin email addresses | | access.roles | No | Array of allowed user roles | | access.allowIpAddresses | No | IP allowlist for Studio requests. Supports exact IPs and wildcard patterns (example: 192.168. ) | | access.blockIpAddresses | No | IP
- `frontend/src/contexts/ThemeContext.tsx:1-85` - StudioTheme, THEME_STORAGE_KEY, ThemeProvider (source; TypeScript; module frontend/src; symbols StudioTheme, THEME_STORAGE_KEY, ThemeProvider, useTheme). import { createContext, type ReactNode, useContext, useEffect, useMemo, useState } from "react"; export type StudioTheme = "light" | "dark"; export const THEME STORAGE KEY = "better auth studio theme"; type ThemeContextValue = { theme: StudioTheme; setTheme: (theme: StudioTheme) = void; toggleTheme: () = void; }; const ThemeContext = createContext (null); function getConfiguredTheme(): StudioTheme { const configuredT

### Source Details
- `examples/tanstack-start/drizzle/src/components/meta-theme.ts`: language TypeScript; exports `MetaTheme`
- `frontend/src/contexts/ThemeContext.tsx`: language TypeScript; exports `StudioTheme`, `THEME_STORAGE_KEY`, `ThemeProvider`, `useTheme`

### Evidence
- `examples/tanstack-start/drizzle/src/components/meta-theme.ts`
- `frontend/src/contexts/ThemeContext.tsx`
- `README.md#configuration-options`
