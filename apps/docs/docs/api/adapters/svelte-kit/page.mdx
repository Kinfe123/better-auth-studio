---
title: "SvelteKit Adapter"
description: "The package exposes a SvelteKit adapter surface."
order: 740
---

# SvelteKit Adapter

The package exposes a SvelteKit adapter surface.
## What This Is
SvelteKit Adapter is documented from the source evidence attached to this page. The goal is to explain what a developer can rely on, what the code detects or returns, and what behavior the utility exposes before listing raw file paths.

## Why It Matters
SvelteKit Adapter matters when a reader needs to understand the behavior exposed by Better Auth Studio without reverse-engineering the implementation first.
## What It Actually Does
- `betterAuthStudio(config: StudioConfig)` - SvelteKit adapter for Better Auth Studio Usage in a catch-all route handler: ```ts // src/routes/api/studio/[...path]/+server.ts import { betterAuthStudio } from 'better-auth-studio/svelte-kit'; import studioConfig from '../../../../studio.config'; const handler = betterAuthStudio(studioConfig); export async function GET(event) { return handler(event); } export async function POST(event) { return handler(event); } export async function PUT(event) { return handler(event); } export async function DELETE(event) { return handler(event); } export async function PATCH(event) { return handler(event); } ```

## Behavior From Source Evidence
- Recognizes evidence markers such as `name`, `version`, `description`, `license`, `author`, `better-auth-studio`, `type`, `main`, `./express`, `./nextjs`, `./hono`, `./elysia`, `./svelte-kit`, `./solid-start`, and more.
- Maps detected inputs to normalized values such as `name` -> `better-auth-studio`, `version` -> `1.1.3-beta.49`, `description` -> `Studio for Better Auth`, `license` -> `MIT`, `author` -> `kinfish`, `better-auth-studio` -> `./dist/cli.js`, `type` -> `module`, `main` -> `dist/index.js`, and more.
## Import Surface
Use `better-auth-studio/svelte-kit` as the package path indicated by package metadata or adapter file evidence.
## API Mental Model
Use the source-backed behavior above as the contract. The related exports below identify the functions or types a maintainer should keep aligned with the docs.
## Related Exports
- `auth`
- `authClient`
- `betterAuthStudio`
- `handle`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/svelte-kit/.eslintrc.cjs` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/svelte-kit/mongodb/app.d.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/svelte-kit/mongodb/postcss.config.js` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/svelte-kit/mongodb/src/hooks.server.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/svelte-kit/mongodb/src/lib/auth-client.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/svelte-kit/mongodb/src/lib/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `src/adapters/svelte-kit.ts` - Framework adapter that connects host requests to the product runtime.

### Knowledge Chunks
- `package.json:1-64` - Package Config (package; JSON; heading Package). { "name": "better auth studio", "version": "1.1.3 beta.49", "description": "Studio for Better Auth", "keywords": [ "admin", "authentication", "better auth", "dashboard", "gui", "studio" ], "license": "MIT", "author": "kinfish", "bin": { "better auth studio": "./dist/cli.js" }, "files": [ "README.md", "data/default geo.json", "dist/", "scripts/postinstall.js" ], "type": "module", "main": "dist/index.js", "exports": {
- `package.json:64-116` - Package Config 2 (package; JSON; heading Package). "release:canary": "pnpm build && bumpp preid canary no commit no tag no push && pnpm publish access public tag canary no git checks", "bump": "bumpp" }, "dependencies": { "@babel/preset react": "^7.27.1", "@babel/preset typescript": "^7.27.1", "@better auth/utils": "^0.3.0", "@noble/hashes": "^2.0.1", "@number flow/react": "^0.5.10", "@types/chokidar": "^2.1.7", "better auth": "1.4.5", "bumpp": "^10.2.3", "c12": "^3.
- `src/adapters/svelte-kit.ts:1-52` - betterAuthStudio (source; TypeScript; module adapters/svelte-kit; symbols betterAuthStudio). // @ts expect error SvelteKit types may not be available in the main package import type { RequestEvent } from "@sveltejs/kit"; import { handleStudioRequest } from "../core/handler.js"; import type { StudioConfig, UniversalRequest, UniversalResponse } from "../types/handler.js"; import { injectEventHooks, injectLastSeenAtHooks } from "../utils/hook injector.js"; / SvelteKit adapter for Better Auth Studio Usage in a c
- `src/adapters/svelte-kit.ts:52-129` - betterAuthStudio (source; TypeScript; module adapters/svelte-kit; symbols betterAuthStudio). return new Response(JSON.stringify({ error: "Internal server error" }), { status: 500, headers: { "Content Type": "application/json" }, }); } }; } async function convertSvelteKitToUniversal( event: RequestEvent, config: StudioConfig, ): Promise { let body: any; const method = event.request.method; if (method !== "GET" && method !== "HEAD") { const contentType = event.request.headers.get("content type") || ""; if (con

### Source Details
- `examples/svelte-kit/mongodb/src/hooks.server.ts`: language TypeScript; exports `handle`
- `examples/svelte-kit/mongodb/src/lib/auth-client.ts`: language TypeScript; exports `authClient`
- `examples/svelte-kit/mongodb/src/lib/auth.ts`: language TypeScript; exports `auth`; env `BETTER_AUTH_URL`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
- `src/adapters/svelte-kit.ts`: language TypeScript; exports `betterAuthStudio`

### Evidence
- `examples/svelte-kit/.eslintrc.cjs`
- `examples/svelte-kit/mongodb/app.d.ts`
- `examples/svelte-kit/mongodb/postcss.config.js`
- `examples/svelte-kit/mongodb/src/hooks.server.ts`
- `examples/svelte-kit/mongodb/src/lib/auth-client.ts`
- `examples/svelte-kit/mongodb/src/lib/auth.ts`
- `package.json:exports:./dist/adapters/svelte-kit.js`
- `package.json:exports:./svelte-kit`
- `src/adapters/svelte-kit.ts`
