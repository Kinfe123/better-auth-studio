---
title: "SolidStart Adapter"
description: "The package exposes a SolidStart adapter surface."
order: 730
---

# SolidStart Adapter

The package exposes a SolidStart adapter surface.
## What This Is
The SolidStart adapter is the framework-specific bridge between the host application and this package. It explains how requests enter the product, where the handler is mounted, and which package export developers import when integrating with SolidStart.
## Why It Matters
SolidStart Adapter matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- SolidStart
- framework adapter
- request handler
- mount path
- package export
## Typical Workflow
1. Import the adapter from the SolidStart-specific package export.
2. Pass the product configuration into the adapter or handler factory.
3. Mount the generated handler at the route expected by the host application.
## Detailed API Contract
SolidStart Adapter is a developer contract, not just an exported name. The page explains what the API represents, what problem it solves, what a caller provides, what the package returns or performs, and how it relates to the surrounding runtime.

API pages are most useful when they explain the shape of responsibility. For an adapter, that means the host framework owns routing and the package supplies a handler. For configuration, it means the host app supplies options and the package changes runtime behavior. For utilities, it means the function supports a feature that needs to be understood before the utility is changed.

## Integration Questions
- What does the caller provide?
- What does the package create, mutate, or return?
- Which feature or configuration page explains the user-facing behavior behind this API?
- What needs to be tested after this API changes?

## Success Criteria
- Readers understand the API purpose before looking at source files.
- Readers know whether the API is public integration surface or internal support code.
- Readers know which runtime or feature depends on it.
## Import Surface
Use `better-auth-studio/solid-start` as the package path indicated by package metadata or adapter file evidence.
## API Mental Model
Adapter APIs work as framework entrypoints: they accept product configuration, produce a request handler, and let the host app decide where that handler is mounted.
## Related Exports
- `betterAuthStudio`
- `clientEnv`
- `clientScheme`
- `Counter`
- `formatErrors`
- `serverScheme`
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Inputs, outputs, side effects, and ownership boundaries are described before exported names.
- The API connects back to feature or configuration pages that explain user-facing behavior.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/solid-start/postcss.config.cjs` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/components/Counter.tsx` - Counter reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/solid-start/src/entry-client.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/entry-server.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/client.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/schema.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `src/adapters/solid-start.ts` - Framework adapter that connects host requests to the product runtime.

### Knowledge Chunks
- `package.json:1-64` - Package Config (package; JSON; heading Package). { "name": "better auth studio", "version": "1.1.3 beta.49", "description": "Studio for Better Auth", "keywords": [ "admin", "authentication", "better auth", "dashboard", "gui", "studio" ], "license": "MIT", "author": "kinfish", "bin": { "better auth studio": "./dist/cli.js" }, "files": [ "README.md", "data/default geo.json", "dist/", "scripts/postinstall.js" ], "type": "module", "main": "dist/index.js", "exports": {
- `package.json:64-116` - Package Config 2 (package; JSON; heading Package). "release:canary": "pnpm build && bumpp preid canary no commit no tag no push && pnpm publish access public tag canary no git checks", "bump": "bumpp" }, "dependencies": { "@babel/preset react": "^7.27.1", "@babel/preset typescript": "^7.27.1", "@better auth/utils": "^0.3.0", "@noble/hashes": "^2.0.1", "@number flow/react": "^0.5.10", "@types/chokidar": "^2.1.7", "better auth": "1.4.5", "bumpp": "^10.2.3", "c12": "^3.

### Source Details
- `examples/solid-start/src/components/Counter.tsx`: language TypeScript; exports `Counter`
- `examples/solid-start/src/env/client.ts`: language TypeScript; exports `clientEnv`, `formatErrors`
- `examples/solid-start/src/env/schema.ts`: language TypeScript; exports `clientScheme`, `serverScheme`; env `VERCEL`, `VERCEL_URL`
- `src/adapters/solid-start.ts`: language TypeScript; exports `betterAuthStudio`

### Evidence
- `examples/solid-start/postcss.config.cjs`
- `examples/solid-start/src/components/Counter.tsx`
- `examples/solid-start/src/entry-client.tsx`
- `examples/solid-start/src/entry-server.tsx`
- `examples/solid-start/src/env/client.ts`
- `examples/solid-start/src/env/schema.ts`
- `package.json:exports:./dist/adapters/solid-start.js`
- `package.json:exports:./solid-start`
- `src/adapters/solid-start.ts`
