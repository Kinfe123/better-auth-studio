---
title: "SolidStart Adapter"
description: "The package exposes a SolidStart adapter surface."
order: 730
---

# SolidStart Adapter

The package exposes a SolidStart adapter surface.
## What This Is
SolidStart Adapter is documented from the source evidence attached to this page. The goal is to explain what a developer can rely on, what the code detects or returns, and what behavior the utility exposes before listing raw file paths.

## Why It Matters
SolidStart Adapter matters when a reader needs to understand the behavior exposed by Better Auth Studio without reverse-engineering the implementation first.
## What It Actually Does
- `betterAuthStudio(config: StudioConfig)` - SolidStart adapter for Better Auth Studio Usage in a catch-all route handler: ```ts // src/routes/api/studio/[...path].ts import { betterAuthStudio } from 'better-auth-studio/solid-start'; import studioConfig from '../../../../studio.config'; const handler = betterAuthStudio(studioConfig); export const GET = handler; export const POST = handler; export const PUT = handler; export const DELETE = handler; export const PATCH = handler; ```

## Behavior From Source Evidence
- Recognizes evidence markers such as `name`, `version`, `description`, `license`, `author`, `better-auth-studio`, `type`, `main`, `./express`, `./nextjs`, `./hono`, `./elysia`, `./svelte-kit`, `./solid-start`, and more.
- Maps detected inputs to normalized values such as `name` -> `better-auth-studio`, `version` -> `1.1.3-beta.49`, `description` -> `Studio for Better Auth`, `license` -> `MIT`, `author` -> `kinfish`, `better-auth-studio` -> `./dist/cli.js`, `type` -> `module`, `main` -> `dist/index.js`, and more.
## Import Surface
Use `better-auth-studio/solid-start` as the package path indicated by package metadata or adapter file evidence.
## API Mental Model
Use the source-backed behavior above as the contract. The related exports below identify the functions or types a maintainer should keep aligned with the docs.
## Related Exports
- `betterAuthStudio`
- `clientEnv`
- `clientScheme`
- `Counter`
- `formatErrors`
- `serverScheme`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/solid-start/postcss.config.cjs` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/components/Counter.tsx` - Counter reusable UI component. It likely renders part of the workflow or a focused interaction.
- `examples/solid-start/src/entry-client.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/entry-server.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/client.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/schema.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `src/adapters/solid-start.ts` - Framework adapter that connects host requests to the product runtime.

### Knowledge Chunks
- `package.json:1-64` - Package Config (package; JSON; heading Package). { "name": "better auth studio", "version": "1.1.3 beta.49", "description": "Studio for Better Auth", "keywords": [ "admin", "authentication", "better auth", "dashboard", "gui", "studio" ], "license": "MIT", "author": "kinfish", "bin": { "better auth studio": "./dist/cli.js" }, "files": [ "README.md", "data/default geo.json", "dist/", "scripts/postinstall.js" ], "type": "module", "main": "dist/index.js", "exports": {
- `package.json:64-116` - Package Config 2 (package; JSON; heading Package). "release:canary": "pnpm build && bumpp preid canary no commit no tag no push && pnpm publish access public tag canary no git checks", "bump": "bumpp" }, "dependencies": { "@babel/preset react": "^7.27.1", "@babel/preset typescript": "^7.27.1", "@better auth/utils": "^0.3.0", "@noble/hashes": "^2.0.1", "@number flow/react": "^0.5.10", "@types/chokidar": "^2.1.7", "better auth": "1.4.5", "bumpp": "^10.2.3", "c12": "^3.
- `src/adapters/solid-start.ts:1-45` - betterAuthStudio (source; TypeScript; module adapters/solid-start; symbols betterAuthStudio). import { handleStudioRequest } from "../core/handler.js"; import type { StudioConfig, UniversalRequest, UniversalResponse } from "../types/handler.js"; import { injectEventHooks, injectLastSeenAtHooks } from "../utils/hook injector.js"; type ApiEvent = { request: Request; params: Record ; locals: Record ; env: Record ; fetch: typeof fetch; url: URL; }; / SolidStart adapter for Better Auth Studio Usage in a catch all
- `src/adapters/solid-start.ts:45-125` - betterAuthStudio (source; TypeScript; module adapters/solid-start; symbols betterAuthStudio). return new Response(JSON.stringify({ error: "Internal server error" }), { status: 500, headers: { "Content Type": "application/json" }, }); } }; } async function convertSolidStartToUniversal( event: ApiEvent, config: StudioConfig, ): Promise { const request = event.request; let body: any; const method = request.method; if (method !== "GET" && method !== "HEAD") { const contentType = request.headers.get("content type"
- `examples/solid-start/postcss.config.cjs:1-7` - Solid Start (example; javascript; module examples/solid-start). module.exports = { plugins: { tailwindcss: {}, autoprefixer: {}, }, };
- `examples/solid-start/src/components/Counter.tsx:1-14` - Counter (example; TypeScript; module examples/solid-start; symbols Counter). import { createSignal } from "solid js"; export default function Counter() { const [count, setCount] = createSignal(0); return ( setCount(count() + 1)} Clicks: {count()} ); }
- `examples/solid-start/src/entry-client.tsx:1-4` - Solid Start (example; tsx; module examples/solid-start). import { mount, StartClient } from "solid start/entry client"; mount(() = , document);
- `examples/solid-start/src/entry-server.tsx:1-4` - Solid Start (example; tsx; module examples/solid-start). import { StartServer, createHandler, renderAsync } from "solid start/entry server"; export default createHandler(renderAsync((event) = ));
- `examples/solid-start/src/env/client.ts:1-19` - clientEnv, formatErrors (example; TypeScript; module examples/solid-start; symbols clientEnv, formatErrors). import type { ZodFormattedError } from "zod"; import { clientScheme } from "./schema"; export const formatErrors = (errors: ZodFormattedError , string ) = Object.entries(errors) .map(([name, value]) = { if (value && " errors" in value) return ${name}: ${value. errors.join(", ")}\n; }) .filter(Boolean); const env = clientScheme.safeParse(import.meta.env); if (env.success === false) { console.error("❌ Invalid environme
- `examples/solid-start/src/env/schema.ts:1-26` - clientScheme, serverScheme (example; TypeScript; module examples/solid-start; symbols clientScheme, serverScheme). import { isServer } from "solid js/web"; import { z } from "zod"; export const serverScheme = z.object({ NODE ENV: z.enum(["development", "production", "test"]).default("development"), AUTH SECRET: z.string(), DATABASE URL: z.string(), START BASE URL: z.preprocess( // This makes Vercel deployments not fail if you don't set START BASE URL // Since NextAuth.js automatically uses the VERCEL URL if present. (str) = proce

### Source Details
- `examples/solid-start/src/components/Counter.tsx`: language TypeScript; exports `Counter`
- `examples/solid-start/src/env/client.ts`: language TypeScript; exports `clientEnv`, `formatErrors`
- `examples/solid-start/src/env/schema.ts`: language TypeScript; exports `clientScheme`, `serverScheme`; env `VERCEL`, `VERCEL_URL`
- `src/adapters/solid-start.ts`: language TypeScript; exports `betterAuthStudio`

### Evidence
- `examples/solid-start/postcss.config.cjs`
- `examples/solid-start/src/components/Counter.tsx`
- `examples/solid-start/src/entry-client.tsx`
- `examples/solid-start/src/entry-server.tsx`
- `examples/solid-start/src/env/client.ts`
- `examples/solid-start/src/env/schema.ts`
- `package.json:exports:./dist/adapters/solid-start.js`
- `package.json:exports:./solid-start`
- `src/adapters/solid-start.ts`
