---
title: "Hono Adapter"
description: "The package exposes a Hono adapter surface."
order: 690
---

# Hono Adapter

The package exposes a Hono adapter surface.
## What This Is
Hono Adapter is documented from the source evidence attached to this page. The goal is to explain what a developer can rely on, what the code detects or returns, and what behavior the utility exposes before listing raw file paths.

## Why It Matters
Hono Adapter matters when a reader needs to understand the behavior exposed by Better Auth Studio without reverse-engineering the implementation first.
## What It Actually Does
- `betterAuthStudio(config: StudioConfig)` - Hono adapter for Better Auth Studio

## Behavior From Source Evidence
- Recognizes evidence markers such as `name`, `version`, `description`, `license`, `author`, `better-auth-studio`, `type`, `main`, `./express`, `./nextjs`, `./hono`, `./elysia`, `./svelte-kit`, `./solid-start`, and more.
- Maps detected inputs to normalized values such as `name` -> `better-auth-studio`, `version` -> `1.1.3-beta.49`, `description` -> `Studio for Better Auth`, `license` -> `MIT`, `author` -> `kinfish`, `better-auth-studio` -> `./dist/cli.js`, `type` -> `module`, `main` -> `dist/index.js`, and more.
## Import Surface
Use `better-auth-studio/hono` as the package path indicated by package metadata or adapter file evidence.
## API Mental Model
Use the source-backed behavior above as the contract. The related exports below identify the functions or types a maintainer should keep aligned with the docs.
## Related Exports
- `auth`
- `betterAuthStudio`
- `config`
- `prisma`
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `examples/hono/prisma/src/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `examples/hono/prisma/src/index.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/hono/prisma/src/prisma.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/hono/prisma/studio.config.pg.example.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `examples/hono/prisma/studio.config.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `src/adapters/hono.ts` - Framework adapter that connects host requests to the product runtime.

### Knowledge Chunks
- `package.json:1-64` - Package Config (package; JSON; heading Package). { "name": "better auth studio", "version": "1.1.3 beta.49", "description": "Studio for Better Auth", "keywords": [ "admin", "authentication", "better auth", "dashboard", "gui", "studio" ], "license": "MIT", "author": "kinfish", "bin": { "better auth studio": "./dist/cli.js" }, "files": [ "README.md", "data/default geo.json", "dist/", "scripts/postinstall.js" ], "type": "module", "main": "dist/index.js", "exports": {
- `package.json:64-116` - Package Config 2 (package; JSON; heading Package). "release:canary": "pnpm build && bumpp preid canary no commit no tag no push && pnpm publish access public tag canary no git checks", "bump": "bumpp" }, "dependencies": { "@babel/preset react": "^7.27.1", "@babel/preset typescript": "^7.27.1", "@better auth/utils": "^0.3.0", "@noble/hashes": "^2.0.1", "@number flow/react": "^0.5.10", "@types/chokidar": "^2.1.7", "better auth": "1.4.5", "bumpp": "^10.2.3", "c12": "^3.
- `src/adapters/hono.ts:1-74` - betterAuthStudio (source; TypeScript; module adapters/hono; symbols betterAuthStudio). import type { Context } from "hono"; import type { StatusCode } from "hono/utils/http status.js"; import { handleStudioRequest } from "../core/handler.js"; import type { StudioConfig, UniversalRequest, UniversalResponse } from "../types/handler.js"; import { injectEventHooks, injectLastSeenAtHooks } from "../utils/hook injector.js"; / Hono adapter for Better Auth Studio / export function betterAuthStudio(config: Stud
- `examples/hono/prisma/src/auth.ts:1-4` - auth (example; TypeScript; module examples/hono; symbols auth). import { betterAuth, url } from "better auth"; import { prismaAdapter } from "better auth/adapters/prisma"; import { createAuthMiddleware } from "better auth/api";
- `examples/hono/prisma/src/auth.ts:4-73` - auth (example; TypeScript; module examples/hono; symbols auth). import { organization, admin, emailOTP, phoneNumber } from "better auth/plugins"; import prisma from "./prisma"; const baseURL = process.env.BETTER AUTH URL || "http://localhost:3000"; export const auth = betterAuth({ secret: process.env.AUTH SECRET || "better auth secret 123456789", database: prismaAdapter(prisma, { provider: "postgresql" }), baseURL, basePath: "/api/auth", socialProviders: { github: { clientId: pro
- `examples/hono/prisma/src/auth.ts:73-104` - auth (example; TypeScript; module examples/hono; symbols auth). log("sendInvitationEmail", data, request); }, }), admin({ adminUserIds: ["i66uyfRQar7veXs94jLZ48YKZO8ehxxK"], }), emailOTP({ sendVerificationOTP: async ({ email, otp, type }) = { console.log(Verification OTP email for ${email}: ${otp}); }, }), ], session: { expiresIn: 60 60 24 7, // 7 days updateAge: 60 60 24, // 1 day }, advanced: { crossSubDomainCookies: { enabled: true, }, }, rateLimit: { enabled: true, window: 10
- `examples/hono/prisma/src/index.ts:1-43` - Hono (example; TypeScript; module examples/hono). import "dotenv/config"; import { Hono } from "hono"; import { cors } from "hono/cors"; import { serve } from "@hono/node server"; import { auth } from "./auth"; import { betterAuthStudio } from "better auth studio/hono"; import studioConfig from "../studio.config"; const app = new Hono(); // CORS configuration app.use( " ", cors({ origin: ["http://localhost:3000", "http://localhost:3002"], allowHeaders: ["Content Typ
- `examples/hono/prisma/src/index.ts:43-132` - Hono (example; TypeScript; module examples/hono). const sessionResponse = await auth.api.getSession({ headers: c.req.raw.headers }); const recentAccounts = await ( await auth.$context ).adapter.findMany({ model: "account", where: [{ field: "providerId", value: "github" }], limit: 5, }); const recentUsers = await ( await auth.$context ).adapter.findMany({ model: "user", limit: 5, }); const sessions = await ( await auth.$context ).adapter.findMany({ model: "session",
- `examples/hono/prisma/src/index.ts:132-148` - Hono (example; TypeScript; module examples/hono). resetPassword({ // body: { // token: token, // newPassword: "someonepassword" // } // }) return c.json({ message: "Test", success }); }); // Start server const PORT = parseInt(process.env.PORT || "3000", 10); serve( { fetch: app.fetch, port: PORT, }, (info) = {
- `examples/hono/prisma/src/index.ts:148-151` - Hono (example; TypeScript; module examples/hono). console.log(🚀 Better Auth Test Project running on http://localhost:${info.port}); }, );

### Source Details
- `examples/hono/prisma/src/auth.ts`: language TypeScript; exports `auth`; env `AUTH_SECRET`, `BETTER_AUTH_URL`, `DISCORD_CLIENT_ID`, `DISCORD_CLIENT_SECRET`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`
- `examples/hono/prisma/src/index.ts`: language TypeScript; env `PORT`
- `examples/hono/prisma/src/prisma.ts`: language TypeScript; exports `prisma`; env `NODE_ENV`
- `examples/hono/prisma/studio.config.pg.example.ts`: language TypeScript; exports `config`; env `DATABASE_URL`
- `examples/hono/prisma/studio.config.ts`: language TypeScript; exports `config`; env `CLICKHOUSE_HOST`, `CLICKHOUSE_PASSWORD`, `CLICKHOUSE_USERNAME`, `IPINFO_TOKEN`
- `src/adapters/hono.ts`: language TypeScript; exports `betterAuthStudio`

### Evidence
- `examples/hono/prisma/src/auth.ts`
- `examples/hono/prisma/src/index.ts`
- `examples/hono/prisma/src/prisma.ts`
- `examples/hono/prisma/studio.config.pg.example.ts`
- `examples/hono/prisma/studio.config.ts`
- `package.json:exports:./dist/adapters/hono.js`
- `package.json:exports:./hono`
- `src/adapters/hono.ts`
