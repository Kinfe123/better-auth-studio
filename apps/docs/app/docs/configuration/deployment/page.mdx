---
title: "Deployment"
description: "The repository includes deployment, container, or platform configuration files."
order: 250
---

# Deployment

The repository includes deployment, container, or platform configuration files.
## What This Is
Deployment documentation explains how the project is expected to run outside local development, which platform files are authoritative, and what needs to be verified for preview, staging, and production environments.

Repository context: If you want to run Better Auth Studio in its own container, this repository includes a Docker setup for the standalone CLI server.
## Why It Matters
Deployment matters because it is part of how Better Auth Studio turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses code-level details as supporting context.
## Core Concepts
- deployment
- containers
- platform config
- preview environment
- production
## Typical Workflow
1. Locate Docker, Railway, Vercel, Wrangler, or platform-specific configuration.
2. Connect deployment files to required environment variables and runtime commands.
3. Document verification steps for preview and production environments.
## Detailed Configuration Model
Deployment controls behavior rather than UI copy. The important questions are what decision this configuration helps a developer make, what happens when it is absent, and what runtime behavior changes when it is enabled. This page keeps the mental model ahead of the raw files that mention the setting.

For Better Auth Studio, configuration usually answers practical questions: which services the command or runtime connects to, which secrets or URLs must be present, which defaults are safe for local use, and which values change scale, safety, or output. Each setting is documented in relation to one of those questions.

## Decision Points
- Is the setting required for every installation, or only for a specific framework, adapter, or deployment mode?
- Does the setting affect credentials, persistence, batching, integrations, output, or developer convenience?
- What does a developer verify after changing it?

## Reader Outcomes
- Readers know when to configure this area and when they can ignore it.
- Readers understand the consequence of a wrong value.
- Readers can connect the setting to a visible command result, runtime behavior, or integration.
## Configuration Surface
Configuration is easiest to understand when each setting is tied to the behavior it controls. The tables below collect the relevant files and environment variables after the mental model, so readers can connect the concept to the concrete setup surface.
### Files
- `docker/compose.yml` (deployment) - Container or Docker Compose setup.
- `docker/docker-compose.self-host.yml` (deployment) - Container or Docker Compose setup.
- `Dockerfile` (deployment) - Container or Docker Compose setup.
- `vercel.json` (deployment) - Deployment platform configuration.
### Environment Variables
- `AUTH_SECRET`
- `AUTH_URL`
- `BASE_URL`
- `BETTER_AUTH_ALLOWED_HOSTS`
- `BETTER_AUTH_SECRET`
- `BETTER_AUTH_URL`
- `CLERK_SECRET_KEY`
- `CLICKHOUSE_HOST`
- `CLICKHOUSE_PASSWORD`
- `CLICKHOUSE_USERNAME`
- `DATABASE_URL`
- `DB_HOST`
- `DB_NAME`
- `DB_PASSWORD`
- `DB_PORT`
- `DB_USER`
- `DISCORD_CLIENT_ID`
- `DISCORD_CLIENT_SECRET`
- `GITHUB_CLIENT_ID`
- `GITHUB_CLIENT_SECRET`
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`
- `IPINFO_TOKEN`
- `MONGO_URI`
- `MONGODB_DB_NAME`
- `MONGODB_URI`
- `MYSQL_URL`
- `NEXT_PUBLIC_BETTER_AUTH_URL`
- `NODE_ENV`
- `NODE_PATH`
- `PORT`
- `POSTGRES_URL`
- `RESEND_API_KEY`
- `VERCEL`
- `VERCEL_URL`
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Docker (Standalone Self Host)
If you want to run Better Auth Studio in its own container, this repository includes a Docker setup for the standalone CLI server.

This mode is useful when:

- you want a separate admin container
- your Better Auth app already lives in its own repository
- you want Docker-managed `node_modules` and package-manager cache volumes

> **Important:** The standalone container still needs your Better Auth project mounted into it, because Studio loads your real `auth.ts` and `studio.config.*` files at runtime.

Build the image from this repository:

```bash
docker build -t better-auth-studio:self-host .
```

Run it with Docker Compose:

```bash
HOST_PROJECT_PATH=/absolute/path/to/your-better-auth-project \
CONFIG_PATH=./auth.ts \
docker compose -f docker/compose.yml up --build
```

If your auth config is not at the project root, pass it explicitly:

```bash
HOST_PROJECT_PATH=/absolute/path/to/your-better-auth-project \
CONFIG_PATH=./src/lib/auth.ts \
docker compose -f docker/compose.yml up --build
```

If you already used the older filename, `docker/docker-compose.self-host.yml` still works too.

What the container does:

- mounts your Better Auth project into `/workspace`
- installs project dependencies into Docker volumes on first boot
- starts `better-auth-studio start --host 0.0.0.0 --port 3002 --no-open`

Useful environment variables:

- `HOST_PROJECT_PATH` - absolute path to your Better Auth project on the host machine
- `CONFIG_PATH` - optional auth config path inside the mounted project, for example `./src/lib/auth.ts`
- `PORT` - studio port, defaults to `3002`
- `WATCH` - set to `true` to enable watch mode
- `AUTO_INSTALL` - set to `false` if your container/project already has dependencies installed
- `PROJECT_INSTALL_CMD` - optional custom install command for non-standard setups
- `GEO_DB_PATH` - optional GeoLite database path inside the mounted project

If you change the Studio port, make sure your Better Auth config also trusts that origin when needed. The examples usually include both `http://localhost:3000` and `http://localhost:3002` for that reason.

If you already embed Studio in your server with the framework adapters below, that remains the recommended production setup. The Docker flow is mainly for running the standalone Studio server in a containerized environment.
## How to Reason About This Config
- Identify whether the option affects security, runtime mounting, data access, or presentation.
- Prefer examples that use the same names and defaults found in the repository.
- Call out adapter-specific behavior instead of implying every setting applies to every framework.
## Validation Checklist
- Keep config examples aligned with source defaults and runtime validation.
- Mention whether a setting is required, optional, or only needed for a specific adapter.
- Update this page when config object names, env vars, or supported providers change.
## Usage Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect source files.
- Examples should match the commands, config names, import paths, or behavior present in the repository.
- Code-level context stays at the bottom so the main page remains useful to product readers.
- Required settings, optional settings, defaults, and adapter-specific behavior are separated clearly.
- Credentials, destructive scale controls, and validation expectations are called out before examples.
## Source Reference
Use these links when you need to verify behavior, update examples, or trace a public name back to the code that owns it.

### Key Files
- `docker/compose.yml` - Deployment configuration that explains how the product runs outside local development.
- `docker/docker-compose.self-host.yml` - Deployment configuration that explains how the product runs outside local development.
- `Dockerfile` - Deployment configuration that explains how the product runs outside local development.
- `docs/.vercelignore` - Deployment configuration that explains how the product runs outside local development.
- `docs/vercel.json` - Deployment configuration that explains how the product runs outside local development.
- `vercel.json` - Deployment configuration that explains how the product runs outside local development.

### Public Names and Routes
No source-level declarations, routes, environment variables, or CLI flags are associated with these files.
