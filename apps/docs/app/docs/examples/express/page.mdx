---
title: "Express Example"
description: "Express Example shows the shape of a working Express integration."
order: 180
---

# Express Example

Express Example shows the shape of a working Express integration.
## What This Example Shows
Express Example demonstrates how an Express host app mounts Better Auth Studio, where configuration lives, and which route or server handler connects the app to the runtime.
## Setup Flow
1. Open the Express example app and install its dependencies.
2. Review the auth, product, and framework route files before copying the setup.
3. Run the example locally and verify the mounted route loads against the configured runtime.
## What To Look For
- The framework route or handler that mounts the product runtime.
- The auth or product config file that supplies runtime options.
- The package scripts or framework command used to run the example locally.
