---
title: "SolidStart Example"
description: "SolidStart Example shows the shape of a working SolidStart integration."
order: 380
---

# SolidStart Example

SolidStart Example shows the shape of a working SolidStart integration.
## What This Example Shows
SolidStart Example demonstrates how a SolidStart host app mounts Better Auth Studio, where configuration lives, and which route or server entrypoint connects the app to the Studio runtime.
## Setup Flow
1. Open the SolidStart example app and install its dependencies.
2. Review the auth, Studio, and framework route files before copying the setup.
3. Run the example locally and verify the Studio route loads against the configured Better Auth instance.
## What To Look For
- The framework route or handler that mounts the Studio runtime.
- The auth or Studio config file that supplies runtime options.
- The package scripts or framework command used to run the example locally.
## Supporting Files
Use these files when you need to verify examples or update the behavior behind this page.

- `examples/solid-start/postcss.config.cjs` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/components/Counter.tsx` - Counter reusable UI component. It renders a visible part of the workflow or a focused interaction.
- `examples/solid-start/src/entry-client.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/entry-server.tsx` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/client.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/solid-start/src/env/schema.ts` - Example project file that shows how the capability is wired in a real framework setup.
