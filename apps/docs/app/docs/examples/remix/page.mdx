---
title: "Remix Example"
description: "Remix Example shows the shape of a working Remix integration."
order: 370
---

# Remix Example

Remix Example shows the shape of a working Remix integration.
## What This Example Shows
Remix Example demonstrates how a Remix host app mounts Better Auth Studio, where configuration lives, and which route or server entrypoint connects the app to the Studio runtime.
## Setup Flow
1. Open the Remix example app and install its dependencies.
2. Review the auth, Studio, and framework route files before copying the setup.
3. Run the example locally and verify the Studio route loads against the configured Better Auth instance.
## What To Look For
- The framework route or handler that mounts the Studio runtime.
- The auth or Studio config file that supplies runtime options.
- The package scripts or framework command used to run the example locally.
## Related Files
Open these files when you need to verify examples or update the behavior behind this page.

- `examples/remix/prisma/app/lib/auth.server.ts` - Auth.Server page-level UI. This is where users usually encounter the capability in the product.
- `examples/remix/prisma/app/lib/db.server.ts` - Db.Server page-level UI. This is where users usually encounter the capability in the product.
- `examples/remix/prisma/app/root.tsx` - Root page-level UI. This is where users usually encounter the capability in the product.
- `examples/remix/prisma/app/routes/_index.tsx` - Route or API layer that receives requests and coordinates runtime behavior.
- `examples/remix/prisma/app/routes/api.auth.$.ts` - Route or API layer that receives requests and coordinates runtime behavior.
- `examples/remix/prisma/app/routes/api.studio.$.ts` - Route or API layer that receives requests and coordinates runtime behavior.
