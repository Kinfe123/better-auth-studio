---
title: "Core Backend Systems"
description: "The backend page explains request handling, runtime configuration, data-facing services, and the server-side workflow behind the product."
order: 480
---

# Core Backend Systems

The backend page explains request handling, runtime configuration, data-facing services, and the server-side workflow behind the product.
## What Lives Here
- `src/add-svelte-kit-env-modules.ts` - Repository file related to this page.
- `src/auth-adapter.ts` - Repository file related to this page.
- `src/config.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `src/core/handler.ts` - Handler core runtime file. It usually coordinates the main execution path or shared system behavior.
- `src/data.ts` - Repository file related to this page.
- `src/geo-service.ts` - Repository file related to this page.
- `src/get-tsconfig-info.ts` - Repository file related to this page.
- `src/index.ts` - Package or runtime file that pulls the main system together.
## Operational Shape
- Request handling is implemented across 61 route or API files.
- `BETTER_AUTH_SECRET`
- `AUTH_SECRET`
- `BETTER_AUTH_URL` - URL used by the runtime to reach an external service, app, or callback endpoint.
- `NEXT_PUBLIC_BETTER_AUTH_URL` - URL used by the runtime to reach an external service, app, or callback endpoint.
- `DATABASE_URL` - Database connection string used by the runtime or migration layer.
- `MYSQL_URL` - URL used by the runtime to reach an external service, app, or callback endpoint.
- `NODE_PATH`
- `POSTGRES_URL` - URL used by the runtime to reach an external service, app, or callback endpoint.
- `RESEND_API_KEY` - API key used to authenticate with the named provider.
- `AUTH_URL` - URL used by the runtime to reach an external service, app, or callback endpoint.
## Related Areas
- [Build System and Package Structure](/docs/build/package-structure) - The build page explains how the repository is packaged, built, exported, and prepared for local development or distribution.
- [CLI and Development Tools](/docs/cli/tooling) - The CLI page explains terminal-driven workflows, command targets, development tooling, and the flags developers use while working locally.
- [Documentation Site](/docs/docs/site) - The documentation page explains how the repository's docs site or docs application is structured, built, and connected to the product.
- [Frontend Application](/docs/frontend/application) - The frontend page explains the user-facing application, navigation shell, and interface workflows readers actually interact with.
