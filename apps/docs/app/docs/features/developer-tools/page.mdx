---
title: "Developer Tools"
description: "The project includes developer-facing tools, generators, inspectors, or visual editors."
order: 50
---

# Developer Tools

The project includes developer-facing tools, generators, inspectors, or visual editors.
## What This Is
This section explains the utilities that help developers inspect, configure, or generate parts of the product. These tools are not just UI screens; they are shortcuts for common setup, debugging, schema, email, or integration work.
## When To Use This
- Use this when you want to understand what developer tools lets a user or operator accomplish.
- Use it before changing UI, routes, permissions, or data flows tied to the capability.
- Pair it with setup and configuration pages when the feature depends on runtime values.
## Core Concepts
- developer tools
- generators
- inspectors
- visual editors
- debug workflows
## Typical Workflow
1. Choose the tool that matches the setup or debugging task.
2. Review generated code or detected state before applying it to the host project.
3. Use tool output as a starting point, then validate it against the application's real configuration.
## Where You See It
Use these hints to connect the concept to the parts of the product or package a reader is most likely to touch.

- You will usually see this around `CodeEditor`, `CommandPalette`, `DatabaseVisualizer`, `EmailEditor`, `Tools`.
- The README talks about this near `docker-standalone-self-host`, `installation-for-self-hosting`, `next-js-app-router`.
## User Path
1. Choose the tool that matches the setup or debugging task.
2. Review generated code or detected state before applying it to the host project.
3. Use tool output as a starting point, then validate it against the application's real configuration.
## Expected Outcome
- You understand the feature's main outcome before changing configuration or code.
- You know which permissions, data, or runtime settings can affect the workflow.
- You know where to continue when you need exact commands, imports, or deployment details.
## Related Files
Open these files when you need to verify examples or update the behavior behind this page.

- `frontend/src/pages/DatabaseVisualizer.tsx` - DatabaseVisualizer page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/lib/studio-tools.ts` - Repository file related to this page.
- `frontend/src/pages/EmailEditor.tsx` - EmailEditor page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/Tools.tsx` - Tools page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/components/CodeEditor.tsx` - CodeEditor reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/CommandPalette.tsx` - CommandPalette reusable UI component. It likely renders part of the workflow or a focused interaction.
