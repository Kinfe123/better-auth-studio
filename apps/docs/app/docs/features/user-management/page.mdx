---
title: "User Management"
description: "User management helps operators inspect accounts, profiles, and account-level actions."
order: 90
---

# User Management

User management helps operators inspect accounts, profiles, and account-level actions.
## What This Is
This capability is the administrative surface for user records. It helps an operator inspect accounts, find a user by identity, update profile or verification state, and remove records when needed. In an auth-oriented product, this page is usually one of the highest-value docs pages because it explains the exact model an admin interacts with.
## When To Use This
- Use this page when you are operating, integrating, or changing user management.
- Read it before changing UI, routes, permissions, or data flows tied to this workflow.
- Pair it with setup and configuration pages when runtime values affect what the user sees.
## Core Concepts
- users
- accounts
- profiles
- email verification
- last seen activity
## Typical Workflow
1. Find a user through the list, search, or filter UI.
2. Open the user detail view to inspect profile, account, and session-related data.
3. Apply mutations such as create, edit, delete, or bulk seed only when the configured access policy allows it.
## In The Product
These are the product surfaces or existing docs sections readers are most likely to touch while using this workflow.

- Primary product surfaces: `RecentUsersWidget`, `UserDetails`, `Users`.
- README context: `configuration-options`, `dashboard`, `try-better-auth-studio-demo`, `user-management`.
## User Path
1. Find a user through the list, search, or filter UI.
2. Open the user detail view to inspect profile, account, and session-related data.
3. Apply mutations such as create, edit, delete, or bulk seed only when the configured access policy allows it.
## Expected Outcome
- You can describe the workflow in product terms before changing configuration or code.
- You know which permissions, data, or runtime settings can change the user's experience.
- You know where to continue when you need exact commands, imports, or deployment details.
## From The README
The README already describes this area in project language. Use these excerpts as the reader-facing baseline before adding more examples.

### 👥 User Management
- View all users - Paginated list with search and filtering
- Create users - Add new users with email/password
- Edit users - Update user information, email verification status
- Delete users - Remove users from the system
- Bulk operations - Seed multiple test users
- User details - View user profiles, and accounts
## Supporting Files
Use these files when you need to verify examples or update the behavior behind this page.

- `frontend/src/components/dashboard-widgets/RecentUsersWidget.tsx` - RecentUsersWidget reusable UI component. It renders a visible part of the workflow or a focused interaction.
- `frontend/src/pages/Users.tsx` - Users page-level UI. This is where users usually encounter the capability in the product.
- `frontend/src/pages/UserDetails.tsx` - UserDetails page-level UI. This is where users usually encounter the capability in the product.
- `examples/tanstack-start/drizzle/src/routes/account/$path.tsx` - Route or API layer that receives requests and coordinates runtime behavior.
