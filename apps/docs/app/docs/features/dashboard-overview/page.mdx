---
title: "Dashboard Overview"
description: "The project exposes dashboard or overview UI for operational visibility."
order: 40
---

# Dashboard Overview

The project exposes dashboard or overview UI for operational visibility.
## What This Is
This area gives operators a high-level view of the system before they drill into individual records. It usually brings together counts, recent activity, database state, and health signals so an admin can understand whether the product is working as expected.
## When To Use This
- Use this when you want to understand what dashboard overview lets a user or operator accomplish.
- Use it before changing UI, routes, permissions, or data flows tied to the capability.
- Pair it with setup and configuration pages when the feature depends on runtime values.
## Core Concepts
- workspace status
- operational metrics
- recent activity
- dashboard widgets
## Typical Workflow
1. Open the dashboard to see the current state of the product.
2. Use summary widgets to identify the area that needs attention.
3. Navigate from a widget into the detailed feature page or record list.
## Where You See It
Use these hints to connect the concept to the parts of the product or package a reader is most likely to touch.

- You will usually see this around `Dashboard`, `DashboardCustomizePanel`, `DashboardFloatingPanel`, `DashboardWidgetGrid`, `DatabaseWidget`, `DropTargetSlot`, `EventsWidget`.
- The README talks about this near `dashboard`.
## User Path
1. Open the dashboard to see the current state of the product.
2. Use summary widgets to identify the area that needs attention.
3. Navigate from a widget into the detailed feature page or record list.
## Expected Outcome
- You understand the feature's main outcome before changing configuration or code.
- You know which permissions, data, or runtime settings can affect the workflow.
- You know where to continue when you need exact commands, imports, or deployment details.
## From The README
The README already describes this area in project language. Use these excerpts as the reader-facing baseline before adding more examples.

### 📊 Dashboard
- Overview statistics - User, teams and organization counts data
## Related Files
Open these files when you need to verify examples or update the behavior behind this page.

- `frontend/src/components/dashboard-widgets/DatabaseWidget.tsx` - DatabaseWidget reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DashboardCustomizePanel.tsx` - DashboardCustomizePanel reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DashboardFloatingPanel.tsx` - DashboardFloatingPanel reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DashboardWidgetGrid.tsx` - DashboardWidgetGrid reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/DropTargetSlot.tsx` - DropTargetSlot reusable UI component. It likely renders part of the workflow or a focused interaction.
- `frontend/src/components/dashboard-widgets/EventsWidget.tsx` - EventsWidget reusable UI component. It likely renders part of the workflow or a focused interaction.
