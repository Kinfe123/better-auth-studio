---
title: "SvelteKit Setup"
description: "SvelteKit setup shows how to mount the product inside a SvelteKit host application."
order: 360
---

# SvelteKit Setup

SvelteKit setup shows how to mount the product inside a SvelteKit host application.
## What You Are Wiring
This setup page explains how to run the product inside a SvelteKit application. It covers the runtime dependency, the framework route or server hook, and the configuration the host app must provide.
## Runtime Shape
This page focuses on the SvelteKit runtime path provided by adapters, examples, or package exports.
Use `better-auth-studio/svelte-kit` when wiring this runtime from the package export.
## Integration Story
For SvelteKit, the important idea is that the host application owns runtime state, database access, routing, and deployment. Better Auth Studio provides the handler or adapter that plugs into that host application. This page makes that boundary clear so developers know what the package supplies and what their app must still configure.
## Setup Checklist
1. Install dependencies with `pnpm install`.
2. Configure required environment variables before starting the runtime.
3. Run `pnpm dev` or the framework-specific command from the host app.
4. Verify the mounted route, API handler, or container can reach the same runtime and database configuration as the host app.
## Configuration Files
- `examples/svelte-kit/mongodb/src/lib/auth.ts` (product) - Product runtime configuration.
- `examples/svelte-kit/prisma/src/lib/auth.ts` (product) - Product runtime configuration.
- `examples/svelte-kit/prisma/studio.config.ts` (product) - Product runtime configuration.
- `examples/svelte-kit/mongodb/svelte.config.js` (framework) - SvelteKit application configuration.
- `examples/svelte-kit/mongodb/vite.config.ts` (framework) - Vite application configuration.
- `examples/svelte-kit/prisma/svelte.config.js` (framework) - SvelteKit application configuration.
- `examples/svelte-kit/prisma/vite.config.ts` (framework) - Vite application configuration.
- `examples/svelte-kit/mongodb/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `examples/svelte-kit/mongodb/postcss.config.js` (styling) - Styling and CSS pipeline configuration.
- `examples/svelte-kit/mongodb/tailwind.config.js` (styling) - Styling and CSS pipeline configuration.
## Environment Variables
- `ADMIN_EMAIL_1`
- `ADMIN_EMAIL_2`
- `ADMIN_EMAIL_3`
- `CLICKHOUSE_HOST`
- `IPINFO_TOKEN` - Token used to authenticate a runtime, bot, webhook, or provider integration.
- `PORT`
- `STUDIO_BASE_PATH`
- `BETTER_AUTH_SECRET`
