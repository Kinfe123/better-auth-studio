---
title: "Next.js Setup"
description: "The repository includes a Next.js adapter or example for self-hosted usage."
order: 150
---

# Next.js Setup

The repository includes a Next.js adapter or example for self-hosted usage.
## What This Is
This setup page explains how to run the product inside a Next.js application. It covers the runtime dependency, the framework route or server hook, and the configuration the host app must provide.

⚠️ Beta Feature : Self hosting is currently in beta.  Please report any issues on GitHub.
## When To Use This
- Use this when you want to run Better Auth Studio inside your own application or infrastructure.
- Use it to separate what the host app owns from what the package provides.
- Review environment and deployment settings before exposing the runtime in production.
## Core Concepts
- Next.js
- self-hosting
- runtime dependency
- route mount
- host app config
## Typical Workflow
1. Install the package where the production server can import it.
2. Create the Next.js route, handler, or server integration.
3. Verify the product can read the same runtime, database, and access configuration as the host app.
## Runtime Shape
This page focuses on the Next.js runtime path provided by adapters, examples, or package exports.
Use `better-auth-studio/nextjs` when wiring this runtime from the package export.
## Integration Story
For Next.js, the important idea is that the host application owns runtime state, database access, routing, and deployment. Better Auth Studio provides the handler or adapter that plugs into that host application. This page makes that boundary clear so developers know what the package supplies and what their app must still configure.
## Setup Checklist
1. Install dependencies with `pnpm install`.
2. Configure required environment variables before starting the runtime.
3. Run `pnpm start` or the framework-specific command from the host app.
4. Verify the mounted route, API handler, or container can reach the same runtime and database configuration as the host app.
## From The README
The README already describes this area in project language. Use these excerpts as the reader-facing baseline before adding more examples.

### 🏠 Self Hosting (Beta)
⚠️ Beta Feature : Self hosting is currently in beta.  Please report any issues on GitHub.

### Installation for Self Hosting
Important: For self hosting, install better auth studio as a regular dependency (not devDependency) since it's required at runtime in production:
## Configuration Files
- `examples/nextjs/drizzle/lib/auth.ts` (product) - Product runtime configuration.
- `examples/nextjs/drizzle/studio.config.ts` (product) - Product runtime configuration.
- `examples/nextjs/mongodb/lib/auth.ts` (product) - Product runtime configuration.
- `examples/nextjs/prisma/lib/auth.ts` (product) - Product runtime configuration.
- `examples/nextjs/prisma/studio.config.ts` (product) - Product runtime configuration.
- `examples/express/drizzle/drizzle.config.ts` (database) - Database schema or adapter configuration.
- `examples/nextjs/drizzle/drizzle.config.ts` (database) - Database schema or adapter configuration.
- `examples/nextjs/drizzle/next.config.js` (framework) - Next.js application configuration.
- `examples/nextjs/mongodb/next.config.js` (framework) - Next.js application configuration.
- `examples/nextjs/prisma/next.config.js` (framework) - Next.js application configuration.
- `examples/nextjs/drizzle/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `examples/nextjs/mongodb/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `examples/nextjs/prisma/package.json` (package) - Package metadata, scripts, dependencies, and exports.
## Environment Variables
- `BETTER_AUTH_ALLOWED_HOSTS`
- `CLICKHOUSE_HOST`
- `DB_HOST`
- `NEXT_PUBLIC_BETTER_AUTH_URL`
- `BETTER_AUTH_SECRET`
- 30 more environment variables appear elsewhere in the project; document them on the page for the workflow that uses them.
## Related Files
Open these files when you need to verify examples or update the behavior behind this page.

- `examples/nextjs/drizzle/app/api/auth/[...all]/route.ts` - Route or API layer that receives requests and coordinates runtime behavior.
- `examples/nextjs/drizzle/app/api/studio/[[...path]]/route.ts` - Route or API layer that receives requests and coordinates runtime behavior.
- `examples/nextjs/drizzle/app/layout.tsx` - Layout page-level UI. This is where users usually encounter the capability in the product.
- `examples/nextjs/drizzle/app/page.tsx` - Page page-level UI. This is where users usually encounter the capability in the product.
- `examples/nextjs/drizzle/auth-schema.ts` - Example project file that shows how the capability is wired in a real framework setup.
- `examples/nextjs/drizzle/drizzle.config.ts` - Example project file that shows how the capability is wired in a real framework setup.
